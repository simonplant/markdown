{
  "description": "Feature backlog",
  "items": [
    {
      "id": "FEAT-036",
      "title": "Phase4-extended-doctor: Extended doctor diagnostic rules",
      "intent": "The document doctor catches more structural and stylistic issues: passive voice, orphaned sections, inconsistent list markers. The doctor grows from a basic tool into a genuinely useful writing assistant.",
      "description": "Extend the doctor engine (FEAT-011) with additional diagnostic rules:\n\n- **Passive voice detection**: flag sentences using passive constructions, with a suggestion to rephrase\n- **Orphaned sections**: detect heading-only sections with no content between them\n- **Inconsistent list markers**: detect mixing of -, *, and + within the same list level\n- **Long paragraphs**: flag paragraphs over a configurable word count (default 300 words) as potentially needing a break\n\nAll rules are on by default but individually disablable. Rules produce Diagnostic structs with span, severity, rule ID, and message, consistent with FEAT-011's architecture.",
      "priority": "could",
      "category": "core",
      "steps": [
        "Implement passive voice detection rule in markdown-core doctor module",
        "Implement orphaned sections rule (heading followed immediately by another heading with no content)",
        "Implement inconsistent list markers rule",
        "Implement long paragraph rule with configurable threshold",
        "Add unit tests for each new rule",
        "Wire new rules into the doctor's rule registry",
        "Surface diagnostics in the editor via CodeMirror lint or inline decorations"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test --workspace passes",
          "verify": "cargo test --workspace --quiet 2>&1 | tail -3"
        },
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "Passive voice constructions are flagged with a suggestion",
        "Empty sections (heading with no content before next heading) are flagged",
        "Mixed list markers within the same list level are flagged",
        "Long paragraphs are flagged with the word count"
      ],
      "scope": [
        "markdown-core/src/doctor.rs",
        "src/editor.ts"
      ],
      "dependsOn": [
        "FEAT-011"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": null,
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-037",
      "title": "Phase4-mermaid: Mermaid diagram rendering",
      "intent": "Mermaid fenced code blocks render as interactive diagrams inline in the editor. AI-generated documents with diagrams render beautifully on open. (AI-assisted Mermaid editing is deferred to the post-v1.0 AI phase per D-ROAD-3.)",
      "description": "Render Mermaid diagrams inline in the editor when a fenced code block is tagged with ```mermaid. Use the Mermaid.js library to render the diagram as an SVG, displayed via a CodeMirror widget decoration that replaces the code block in rich view (or shows below it in source view).\n\nAI-assisted Mermaid editing: when the user selects a Mermaid block and invokes an AI action, the AI can modify the Mermaid syntax and the diagram re-renders live.\n\nScope boundary: rendering and basic AI editing. No visual drag-and-drop diagram editor.",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Add mermaid.js as an npm dependency",
        "Detect ```mermaid fenced code blocks in the CodeMirror syntax tree",
        "Render the Mermaid syntax to SVG using mermaid.render()",
        "Display the rendered SVG as a CodeMirror widget decoration below or replacing the code block",
        "Re-render on content change within the mermaid block (debounced)",
        "In rich view (The Render), replace the code block entirely with the rendered diagram"
      ],
      "acceptanceCriteria": [
        {
          "text": "mermaid is in package.json",
          "verify": "grep -q 'mermaid' package.json"
        },
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "A ```mermaid code block renders as an SVG diagram in the editor",
        "Editing the Mermaid syntax updates the diagram in real time",
        "Invalid Mermaid syntax shows an error message instead of a broken diagram"
      ],
      "scope": [
        "src/mermaid.ts",
        "src/editor.ts",
        "package.json"
      ],
      "dependsOn": [
        "FEAT-012"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": "Reconciled 2026-06-19: descoped to render-only for v1.0; AI-assisted Mermaid editing moves to the post-v1.0 AI phase.",
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-038",
      "title": "Phase4-math: Math rendering with KaTeX",
      "intent": "Inline ($...$) and display ($$...$$) math expressions render beautifully in the editor. Math is a first-class content type in technical and academic markdown documents.",
      "description": "Render LaTeX math expressions inline in the editor using KaTeX. Inline math ($...$) renders as styled inline elements. Display math ($$...$$) renders as centered block elements.\n\nUse CodeMirror decorations: in source view, math delimiters are visible and the rendered output appears alongside or on hover. In rich view (The Render), delimiters are hidden and only the rendered math is shown.\n\nScope boundary: rendering only. No equation editor, no MathML export.",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Add katex as an npm dependency",
        "Detect $...$ (inline) and $$...$$ (display) syntax in the editor",
        "Render math expressions using katex.renderToString()",
        "Display inline math as a CodeMirror widget decoration replacing the $...$ span",
        "Display display math as a centered block widget decoration",
        "Handle KaTeX errors gracefully: show the raw syntax with an error indicator",
        "In rich view, hide delimiters and show only rendered math"
      ],
      "acceptanceCriteria": [
        {
          "text": "katex is in package.json",
          "verify": "grep -q 'katex' package.json"
        },
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "Inline math ($x^2$) renders as a formatted math expression",
        "Display math ($$\\sum_{i=1}^n$$) renders as a centered block expression",
        "Invalid LaTeX shows the raw syntax with an error indicator instead of crashing"
      ],
      "scope": [
        "src/math.ts",
        "src/editor.ts",
        "package.json"
      ],
      "dependsOn": [
        "FEAT-012"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": null,
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-039",
      "title": "Phase4-folding: Folding and outline view",
      "intent": "The user can fold sections of the document by heading level and navigate the document structure via an outline panel. Long documents become manageable without scrolling.",
      "description": "Implement section folding and an outline/table-of-contents panel:\n\n1. **Folding**: each heading creates a foldable region spanning to the next heading of equal or higher level. Click a gutter indicator or use a keyboard shortcut to fold/unfold. Folded regions show a placeholder (e.g. '... (N lines)').\n2. **Outline panel**: a side panel showing the document's heading structure as a navigable tree. Clicking a heading in the outline scrolls to it in the editor. The outline updates live as the user edits.\n\nScope boundary: heading-level folding only. No arbitrary fold regions, no manual fold markers.",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Use CodeMirror's foldService to define fold ranges based on heading levels from the markdown syntax tree",
        "Add fold gutters with click-to-fold indicators",
        "Add keyboard shortcuts for fold/unfold (Cmd+Shift+[ / Cmd+Shift+])",
        "Create an outline panel component showing the heading hierarchy",
        "Wire outline clicks to scroll to the corresponding heading in the editor",
        "Update the outline live on document changes",
        "Add a toggle for showing/hiding the outline panel"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "Clicking a fold indicator in the gutter collapses the section under a heading",
        "Folded sections show a placeholder with the number of hidden lines",
        "The outline panel shows all headings in hierarchical order",
        "Clicking a heading in the outline scrolls the editor to that heading"
      ],
      "scope": [
        "src/folding.ts",
        "src/outline.ts",
        "src/editor.ts",
        "src/style.css"
      ],
      "dependsOn": [
        "FEAT-012"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": null,
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-040",
      "title": "Phase4-multi-doc: Multi-document tabs and split view",
      "intent": "The user can open multiple documents in tabs within a single window and optionally split the view to see two documents side by side. This is the progression from the M0 one-file-per-window model to a productivity-oriented multi-document workflow.",
      "description": "Add a tab bar to the editor window. Each tab holds an independent CodeMirror editor instance with its own document state. The user can:\n- Open multiple files (each opens in a new tab)\n- Switch between tabs with keyboard shortcuts (Cmd+Shift+] / Cmd+Shift+[)\n- Close tabs (Cmd+W) with unsaved-changes prompt\n- Drag tabs to reorder\n- Split the view horizontally or vertically to show two tabs side by side\n\nScope boundary: tabs and split view. No file tree sidebar (that could be a separate item).",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Create a tab bar component above the editor area",
        "Manage multiple editor instances, each with independent document state",
        "Opening a file creates a new tab (or focuses existing tab if the file is already open)",
        "Add tab switching shortcuts: Cmd+Shift+] (next), Cmd+Shift+[ (previous), Cmd+1-9 (by index)",
        "Add close-tab behavior with unsaved-changes prompt",
        "Implement drag-to-reorder for tabs",
        "Add split view: a command to split the current view horizontally or vertically",
        "Ensure file watching (FEAT-026) and auto-save (FEAT-025) work correctly per-tab"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "Opening multiple files creates separate tabs",
        "Cmd+Shift+] and Cmd+Shift+[ switch between tabs",
        "Closing a tab with unsaved changes shows a prompt",
        "Split view shows two documents side by side"
      ],
      "scope": [
        "src/tabs.ts",
        "src/editor.ts",
        "src/style.css"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": " Satisfied natively 2026-06-20: DocumentGroup provides multi-document windows/tabs on iPad+Mac; custom in-app tabs would conflict with the OS document model.",
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-041",
      "title": "Phase4-quick-open: Quick Open fuzzy file search",
      "intent": "The user can press Cmd+P to fuzzy-search across all .md files in the current folder tree and open one instantly. This is the fast-switching workflow that power users expect from VS Code and Obsidian.",
      "description": "Implement a Quick Open dialog triggered by Cmd+P (macOS) / Ctrl+P (other platforms). The dialog shows a text input that fuzzy-matches against all .md files in the directory tree containing the current file.\n\nFile discovery: walk the directory tree from the current file's parent directory (or a configured root), collect all .md file paths, and index them for fuzzy matching. Use a simple fuzzy matching algorithm (subsequence match with scoring for consecutive characters and path segment boundaries).\n\nScope boundary: file search and open. No content search (that would be a separate Cmd+Shift+F item).",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Implement a core file-walk (in markdown-core, reached via the binding) that walks a directory tree and returns all .md file paths",
        "Implement a fuzzy matching algorithm in TypeScript (subsequence match with scoring)",
        "Create a Quick Open overlay UI: centered modal with text input and scrollable results list",
        "Wire Cmd+P / Ctrl+P to open the Quick Open overlay",
        "On result selection (Enter or click), open the file (in a new tab if FEAT-040 is implemented, otherwise in the current editor)",
        "Cache the file list and refresh on focus (to catch new files)",
        "Show the relative path of each result for disambiguation"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        {
          "text": "cargo test --workspace passes",
          "verify": "cargo test --workspace --quiet 2>&1 | tail -3"
        },
        "Cmd+P opens a Quick Open overlay",
        "Typing in the overlay fuzzy-matches against .md files in the directory tree",
        "Selecting a result opens the file",
        "Escape closes the overlay without navigation"
      ],
      "scope": [
        "src/quick-open.ts",
        "src/editor.ts",
        "markdown-core/src/lib.rs"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": "Reconciled 2026-06-19: file discovery/IO moved from src-tauri to markdown-core (reached via the WASM/uniffi binding).",
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-042",
      "title": "Phase4-images: Image handling — inline rendering, drag-drop, paste",
      "intent": "Images in markdown (![alt](path)) render inline in the editor. Users can drag-drop or paste images and have them saved to a sensible location with the markdown reference inserted automatically. AI-generated documents with images render beautifully on open.",
      "description": "Implement image handling in the editor:\n\n1. **Inline rendering**: ![alt](path) references render the image inline as a CodeMirror widget decoration. Relative paths resolve from the current file's directory. URLs render via fetch.\n2. **Drag-drop**: dragging an image file onto the editor saves it to the same directory as the current file (or a configurable images/ subdirectory) and inserts ![](relative-path) at the drop position.\n3. **Paste**: pasting an image from the clipboard saves it (with a timestamp-based filename) and inserts the reference.\n4. **Error handling**: broken image references show a placeholder with the alt text and path.\n\nScope boundary: local and URL images. No image editing, no resizing handles, no image optimization.",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Detect ![alt](path) syntax in the CodeMirror syntax tree",
        "Resolve image paths relative to the current file's directory",
        "Render images as CodeMirror widget decorations (replace the syntax with the rendered image in rich view)",
        "Implement drag-drop handler: intercept drop events with image files, save to disk, insert markdown reference",
        "Implement paste handler: intercept paste events with image data, save to disk, insert markdown reference",
        "Handle broken images: show alt text and path as a placeholder",
        "Add max-width styling so large images don't break the layout"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "![alt](path) with a valid local image renders the image inline",
        "Dragging an image file onto the editor inserts a markdown image reference",
        "Pasting an image from the clipboard saves it and inserts a reference",
        "Broken image references show a placeholder, not a broken image icon"
      ],
      "scope": [
        "src/images.ts",
        "src/editor.ts",
        "markdown-core/src/lib.rs"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": "Reconciled 2026-06-19: file discovery/IO moved from src-tauri to markdown-core (reached via the WASM/uniffi binding).",
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-043",
      "title": "Phase4-pdf: Render, print, and share as polished PDF",
      "intent": "The user can export the current document as a beautifully typeset PDF or print it directly. The PDF output should look polished — proper typography, rendered diagrams and math, styled code blocks — not a raw text dump.",
      "description": "Implement PDF export and print for the current document:\n\n1. **PDF export** (Cmd+Shift+E or File > Export as PDF): render the markdown to a styled HTML document, then convert to PDF using the webview's print-to-PDF capability or a library like Puppeteer/Playwright (headless).\n2. **Print** (Cmd+P with Shift, or File > Print): open the OS print dialog with a print-formatted view of the document.\n\nThe output should include: styled headings, bold/italic, rendered code blocks with syntax highlighting, rendered Mermaid diagrams (if FEAT-037), rendered math (if FEAT-038), and images.\n\nScope boundary: single-document export. No batch export, no custom templates.",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Render the current markdown document to styled HTML (reuse The Render's decoration output or a separate render pipeline)",
        "Add a print stylesheet that produces clean, typeset output",
        "Implement Cmd+Shift+E: render to HTML and use the webview's print-to-PDF to save as PDF",
        "Implement File > Print: open the OS print dialog with the styled HTML",
        "Include rendered diagrams and math if those features are available",
        "Add page break hints before top-level headings"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "Cmd+Shift+E exports a PDF file to the user's chosen location",
        "The PDF includes styled headings, bold/italic, and code blocks",
        "File > Print opens the OS print dialog with formatted content",
        "The PDF does not contain raw markdown syntax — it is a rendered view"
      ],
      "scope": [
        "src/pdf.ts",
        "src/editor.ts",
        "src/style-print.css"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": null,
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-044",
      "title": "Phase4-custom-themes: Custom themes and fonts",
      "intent": "Users can customize the editor's appearance beyond the built-in light and dark themes — choose fonts, adjust colors, or install community-created themes. Personalization without a plugin system.",
      "description": "Add a theme customization system:\n\n1. **Font selection**: let the user choose body and code fonts from system-installed fonts, with a preview\n2. **Color scheme customization**: let the user adjust key colors (background, text, accent, heading colors) within a theme\n3. **Community themes**: support loading theme files (JSON or CSS) from a directory (~/.config/markdown/themes/)\n\nTheme files define CSS custom property overrides. The built-in light and dark themes are the defaults; user themes override specific properties.\n\nScope boundary: visual customization only. No editor behavior changes via themes. No theme marketplace.",
      "priority": "could",
      "category": "editor",
      "steps": [
        "Define a theme file format (JSON mapping CSS custom property names to values)",
        "Load user themes from ~/.config/markdown/themes/ on startup",
        "Add a Preferences > Appearance pane with font selection (body and code fonts)",
        "Add color customization controls for key properties (background, text, accent, headings)",
        "Apply theme overrides via CSS custom properties on the root element",
        "Provide a theme preview in the preferences pane",
        "Ensure custom themes work with both light and dark base themes"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm run build succeeds",
          "verify": "npm run build 2>&1 | grep -c 'error' | grep -q '^0$'"
        },
        "A JSON theme file in the themes directory is loaded and applied on startup",
        "Font selection in Preferences changes the editor's body and code fonts",
        "Color customization changes are reflected immediately in the editor",
        "The built-in themes still work when no custom theme is present"
      ],
      "scope": [
        "src/themes.ts",
        "src/settings.ts",
        "src/style.css",
        "src/editor.ts"
      ],
      "dependsOn": [
        "FEAT-014"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": null,
      "groomingNotes": null,
      "phase": "4",
      "epic": null
    },
    {
      "id": "FEAT-045",
      "title": "Roll back AI — gate behind off-by-default cargo feature and remove from UI surface",
      "intent": "PRODUCT D-ROAD-3 and ARCHITECTURE A-AI-DEFERRED commit to shipping AI after v1.0, not before. FEAT-029/030/032 violated that. We want the editor to stop presenting AI as a feature so focus returns to the core reading/writing experience, without throwing away the work — the code stays behind an opt-out flag for when the AI phase formally begins.",
      "description": "Gate the llama.cpp inference engine and all ai_* Tauri commands behind the existing 'ai' cargo feature, and DROP the feature from the default build. Remove aiExtension() registration from src/editor.ts. Remove the AI settings pane from the settings modal (keep BYO-key storage code dormant but unreachable from UI). Remove completionsExtension() registration. The keychain-stored BYO keys must remain untouched on disk for users who already set them. Document the rollback in docs/ARCHITECTURE.md §5 so the AI section matches reality: the constraints stay locked, the implementation is dormant.\n\nScope boundary: no code deletion. This is a visibility/surface rollback, not a git revert. The AI crate features, commands, and modules stay in the tree behind conditional compilation so re-enabling is a one-line build flag change when the AI phase begins.",
      "priority": "must",
      "category": "core",
      "steps": [
        "Remove 'ai' from the default features list in src-tauri/Cargo.toml and markdown-core/Cargo.toml",
        "Wrap all ai_* Tauri command registrations in #[cfg(feature = \"ai\")] in src-tauri/src/lib.rs",
        "Remove aiExtension() and completionsExtension() from the extensions array in src/editor.ts",
        "Remove the AI section from the settings modal UI in src/settings.ts (leave keychain read/write functions intact but unreferenced)",
        "Remove any AI-related status-bar indicators and keyboard shortcuts from the default build",
        "Add a short note to docs/ARCHITECTURE.md §5 explaining the code is gated off pending D-ROAD-3",
        "Verify cargo build --workspace (no --features ai) succeeds and produces a binary with no AI surface",
        "Run the app, confirm no AI menu items, no ghost-text suggestions, no settings pane entries"
      ],
      "acceptanceCriteria": [
        {
          "text": "Default workspace build has no ai feature enabled",
          "verify": "! grep -E '^default\\s*=.*\"ai\"' src-tauri/Cargo.toml markdown-core/Cargo.toml"
        },
        {
          "text": "ai_* Tauri commands are cfg-gated",
          "verify": "grep -B1 'fn ai_' src-tauri/src/lib.rs | grep -q 'cfg(feature = \"ai\")'"
        },
        {
          "text": "aiExtension is not registered in the default editor",
          "verify": "! grep -E '^\\s*aiExtension\\(\\)' src/editor.ts"
        },
        {
          "text": "completionsExtension is not registered in the default editor",
          "verify": "! grep -E '^\\s*completionsExtension\\(\\)' src/editor.ts"
        },
        {
          "text": "Default build succeeds",
          "verify": "cargo build --workspace 2>&1 | tail -3"
        },
        "Running the default build shows no AI-related UI (no ghost text, no AI settings pane, no AI menu items)"
      ],
      "scope": [
        "src-tauri/Cargo.toml",
        "markdown-core/Cargo.toml",
        "src-tauri/src/lib.rs",
        "src/editor.ts",
        "src/settings.ts",
        "docs/ARCHITECTURE.md"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "Solo-dev rollback. Not a revert — cfg-gated so AI phase can re-enable with a feature flag. Watch that the llama-cpp-2 crate does not get pulled in by the default build (check cargo tree after). BYO-key keychain data untouched.",
      "lastFailReason": "Baseline validation failed before developer ran",
      "lastFailAt": "2026-04-21T22:46:44-07:00",
      "failCount": 2,
      "phase": "post-v1",
      "epic": null
    },
    {
      "id": "FEAT-046",
      "title": "Restore committed baseline.json and referenced engineering artifacts",
      "intent": "ARCHITECTURE A-PROC-2 makes docs/baseline.json permanent — it is the regression gate's source of truth. Current git status shows it deleted along with docs/engine-comparison.json, docs/engine-decision.md, docs/commonmark_status.md, and docs/large-file-performance.md. The regression CI job cannot work without baseline.json, and ARCHITECTURE §3.1 cites the engine artifacts as the defense for A-CORE-1. Either restore them or remove the references.",
      "description": "Recover docs/baseline.json (the canonical ubuntu-latest-measured numbers) and the engine-decision artifacts. Preferred path: run the baseline-capture workflow to regenerate baseline.json on the CI runner and commit. For the deleted engine docs, restore from git history if possible, otherwise rewrite brief summaries sufficient to defend A-CORE-1 and document large-file behavior.\n\nScope boundary: this item restores the artifacts. Extending the baseline to the four-slice distribution described in ARCHITECTURE §2 is FEAT-047, not this item.",
      "priority": "must",
      "category": "core",
      "steps": [
        "git log --diff-filter=D to find the commit that deleted each file",
        "git checkout <commit>^ -- docs/baseline.json docs/engine-comparison.json docs/engine-decision.md docs/commonmark_status.md docs/large-file-performance.md",
        "If baseline.json values are stale, trigger the Capture Baseline workflow on main to regenerate",
        "Verify scripts/measure_baseline.sh --check runs cleanly against the restored file",
        "Commit the restored files"
      ],
      "acceptanceCriteria": [
        {
          "text": "docs/baseline.json exists and is valid JSON",
          "verify": "jq . docs/baseline.json >/dev/null"
        },
        {
          "text": "scripts/measure_baseline.sh --check exits 0 against the restored baseline",
          "verify": "bash scripts/measure_baseline.sh --check"
        },
        {
          "text": "Engine-decision artifacts restored",
          "verify": "test -f docs/engine-decision.md && test -f docs/engine-comparison.json"
        },
        {
          "text": "Large-file performance doc restored",
          "verify": "test -f docs/large-file-performance.md"
        }
      ],
      "scope": [
        "docs/baseline.json",
        "docs/engine-comparison.json",
        "docs/engine-decision.md",
        "docs/commonmark_status.md",
        "docs/large-file-performance.md"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "These files were deleted in working tree per git status — they have never been committed-as-deleted. git checkout HEAD -- docs/<file> may be enough. If the Capture Baseline workflow needs to rerun, do that on CI not locally.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "FEAT-047",
      "title": "Baseline corpus with small/medium/large/session slices and per-slice regression gate",
      "intent": "ARCHITECTURE §2 requires the regression gate to fire independently on four representative load slices so that a 12% regression on large-document open time blocks a merge even when small-document numbers look fine. Current measure_baseline.sh only captures open_small_ms and open_10k_ms — no 25 KB medium, no synthetic session, no committed corpus. Without the distribution, D-PERF-1 is not actually enforced.",
      "description": "Create docs/baseline-corpus/ with four representative fixtures: small (~1 KB, ~30 lines), medium (~25 KB, ~800 lines including tables), large (~250 KB, ~8000 lines with rich content), and a synthetic session script (open → scroll → edit → format → save across mixed sizes). Extend scripts/measure_baseline.sh and the measure binary so baseline.json captures each core metric (open, keystroke latency, format, save, reopen) per slice, N≥5 medians. The regression gate must fire on any slice independently at 110% of its committed median.\n\nScope boundary: CJK/RTL/emoji fixtures are a later addition — this item establishes the distribution infrastructure on English content.",
      "priority": "should",
      "category": "core",
      "steps": [
        "Create docs/baseline-corpus/ with small.md, medium.md, large.md and commit",
        "Write a session-script fixture (JSON or shell) describing the synthetic session sequence",
        "Extend markdown-core/bin/measure.rs to accept a corpus path and emit per-slice metrics",
        "Extend docs/baseline.json schema to store per-slice medians",
        "Update scripts/measure_baseline.sh --capture and --check to iterate slices and compare per-slice",
        "Update the baseline-capture CI workflow if the invocation changed",
        "Verify the check fails loudly when an artificial regression is injected into one slice"
      ],
      "acceptanceCriteria": [
        {
          "text": "Four corpus fixtures committed",
          "verify": "test -f docs/baseline-corpus/small.md && test -f docs/baseline-corpus/medium.md && test -f docs/baseline-corpus/large.md"
        },
        {
          "text": "baseline.json contains per-slice metrics",
          "verify": "jq -e '.slices.small and .slices.medium and .slices.large' docs/baseline.json"
        },
        {
          "text": "scripts/measure_baseline.sh --check compares each slice independently",
          "verify": "grep -E 'slice|small|medium|large' scripts/measure_baseline.sh | head -5"
        },
        "Injecting a 15% regression in one slice's open time causes --check to exit non-zero while other slices stay within bounds"
      ],
      "scope": [
        "docs/baseline-corpus/",
        "docs/baseline.json",
        "scripts/measure_baseline.sh",
        "markdown-core/bin/measure.rs",
        ".github/workflows/baseline-capture.yml"
      ],
      "dependsOn": [
        "FEAT-046"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false,
      "groomedAt": "2026-04-21",
      "groomingNotes": "FEAT-047 partial: corpus fixtures committed under docs/baseline-corpus/ (small.md 465B, medium.md 16KB, large.md 200KB), and markdown-core/tests/corpus_smoke.rs gates that they continue to parse. Full per-slice timing integration into scripts/measure_baseline.sh --capture/--check is the remaining scope — the current script captures open_small/open_10k as a proxy distribution, so the baseline gate still fires, but not per-corpus-slice as ARCHITECTURE §2 specifies.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "FEAT-048",
      "title": "CI build matrix — add build-linux, build-windows, build-web jobs",
      "intent": "PRODUCT D-PLAT-1 says every platform is first-class. Current .github/workflows/build.yml has only build-macos and baseline-check. FEAT-022 (Linux) and FEAT-023 (Windows) have failed groom three times because their ACs depend on CI jobs that do not exist. Adding the build matrix unblocks those items and makes cross-platform regressions visible at PR time.",
      "description": "Extend .github/workflows/build.yml with three additional jobs: build-linux (ubuntu-latest, install webkit2gtk-4.1 dev packages, cargo build + tauri build), build-windows (windows-latest, cargo build + tauri build, artifact the MSI), and build-web (ubuntu-latest, npm install + vite build --config vite.config.web.ts, artifact dist-web). Each job runs cargo test --workspace where applicable. The jobs run in parallel with build-macos; failures in any block PR merge.\n\nScope boundary: this item adds the jobs and proves they go green on a trivial PR. Platform-specific features (Spotlight integration, XDG registration, MSI file associations) live in FEAT-022/023.",
      "priority": "must",
      "category": "ci",
      "steps": [
        "Add build-linux job: ubuntu-latest, apt-get install libwebkit2gtk-4.1-dev build-essential libssl-dev, then cargo build --workspace and cargo test --workspace",
        "Add build-windows job: windows-latest, cargo build --workspace and cargo test --workspace",
        "Add build-web job: ubuntu-latest, npm ci and npx vite build --config vite.config.web.ts, upload dist-web as artifact",
        "Ensure all four build jobs run in parallel on pull_request and push to main",
        "Verify the workflow file is valid YAML and commits trigger the new jobs"
      ],
      "acceptanceCriteria": [
        {
          "text": "build-linux job exists in build.yml",
          "verify": "grep -q 'build-linux:' .github/workflows/build.yml"
        },
        {
          "text": "build-windows job exists in build.yml",
          "verify": "grep -q 'build-windows:' .github/workflows/build.yml"
        },
        {
          "text": "build-web job exists in build.yml",
          "verify": "grep -q 'build-web:' .github/workflows/build.yml"
        },
        {
          "text": "build.yml is valid YAML",
          "verify": "python3 -c 'import yaml; yaml.safe_load(open(\".github/workflows/build.yml\"))'"
        },
        "All four build jobs run on a test PR and the matrix completes (may have failures — that's a subsequent fix, not this item's scope)"
      ],
      "scope": [
        ".github/workflows/build.yml"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "Unblocks FEAT-022/023. webkit2gtk-4.1 is the Tauri 2 Linux dependency (not -4.0). If the Linux or Windows build fails on first run due to Tauri-specific setup, file a sub-bug rather than letting this item fail groom — the goal here is the matrix existing, not every platform going green on day one. Reconciled 2026-06-19: the Tauri Linux/Windows/.app jobs this added are superseded by EPIC-CI-REPOINT (PWA primary + Apple uniffi build).",
      "phase": "2",
      "epic": null
    },
    {
      "id": "FEAT-049",
      "title": "Read-mode default view with click-to-enter author mode",
      "intent": "PRODUCT D-UX-1 and DP-3 make read mode the default view when a .md file opens. This is the single most load-bearing product claim in the press release — 'open any .md file and see it rendered beautifully … click to edit and you're in a polished author environment.' Today the editor lands users in author mode. FEAT-015 shipped a source-to-rich toggle animation, but that is an effect on the author surface, not a default read-first mode. Until this ships, the product does not match its own pitch.",
      "description": "Build read mode as a distinct EditorView state over the same document. On file open, mount the read view: all rendered content visible (headings styled, tables laid out, code blocks highlighted, images inline where FEAT-042 ships that), source characters (#, **, backticks) hidden via widget decorations, selection and copy work on rendered text. A single click or tap anywhere in the document transitions to author mode with the cursor placed at the click position. A visible affordance (keyboard shortcut + optional toolbar button) returns to read mode. The transition uses CSS crossfade; prefers-reduced-motion gets an instant crossfade.\n\nScope boundary: this item ships read mode for the content types already rendered in author mode (headings, emphasis, code spans, code blocks, lists, tables, links). Mermaid, math, and images are later items (FEAT-037/038/042) and integrate via whatever rendering hook this item exposes. Remembering per-file last-used mode is out of scope here — can be a follow-up.",
      "priority": "must",
      "category": "editor",
      "steps": [
        "Design the read-mode view as a CodeMirror EditorView variant — reuse WYSIWYM decoration infrastructure in src/wysiwym.ts but apply it globally (cursor-proximity rule does not apply in read mode)",
        "Add a readModeExtension() in a new src/read-mode.ts that hides all syntax marks, disables caret/editing, and enables a single-click handler that dispatches a mode change",
        "Wire the mode state into the top-level app: on file open, mount read mode by default; add Cmd+E (or equivalent) keyboard shortcut to toggle modes",
        "Implement the mode transition: CSS crossfade, Reduced Motion fallback to instant swap",
        "Place cursor at click position when transitioning read→author",
        "Verify keyboard-only users can enter and exit read mode via shortcut (a11y)",
        "Verify screen-reader semantics: read mode presents a document, not a source view"
      ],
      "acceptanceCriteria": [
        {
          "text": "src/read-mode.ts exists and exports readModeExtension",
          "verify": "test -f src/read-mode.ts && grep -q 'readModeExtension' src/read-mode.ts"
        },
        {
          "text": "Opening a .md file mounts read mode by default",
          "verify": "grep -E 'readModeExtension|mode.*=.*read' src/main.ts src/editor.ts"
        },
        {
          "text": "Mode toggle keyboard shortcut is wired",
          "verify": "grep -E 'Mod-e|toggleMode' src/editor.ts src/main.ts"
        },
        "Opening a document with headings, emphasis, code, lists, tables, and links shows rendered output with no visible # / ** / backticks",
        "Single click in read mode enters author mode with cursor placed at the click position",
        "Cmd+E (or platform equivalent) toggles between modes in both directions",
        "prefers-reduced-motion replaces the crossfade with an instant swap",
        "Keyboard-only users can toggle modes without a mouse"
      ],
      "scope": [
        "src/read-mode.ts",
        "src/editor.ts",
        "src/main.ts",
        "src/style.css",
        "index.html"
      ],
      "dependsOn": [
        "FEAT-045"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "Load-bearing. Do not conflate with FEAT-015 (The Render animation) — that is a visual effect on the author surface; this is a distinct default view. The WYSIWYM decoration tree in src/wysiwym.ts is the right starting point for the mark-hiding logic. If scope creeps toward remembering per-file last mode or adding a settings override, split that off — this item ships the default and the transition only.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "FEAT-050",
      "title": "Wire document doctor diagnostics to editor gutter",
      "intent": "PRODUCT D-EDIT-1 makes the document doctor a core on-by-default feature of author mode. ARCHITECTURE §3.4 defines the doctor rules and says results are sent to the editor as diagnostics — underlines and gutter markers. Today markdown-core/src/doctor.rs implements the rules but nothing consumes them: no IPC command, no editor gutter, no underlines. Without this the editor is a dumb text box, not the smart author environment the product claims.",
      "description": "Add a document_diagnose Tauri command that runs the doctor against the current document and returns a list of diagnostics (rule id, severity, byte range, message). Wire the command in the editor frontend to run after edits on a background debounce (500ms after last keystroke). Render diagnostics as CodeMirror gutter markers with hover tooltips showing the message, plus underline decorations on the offending range. The initial rules enabled are those already implemented in doctor.rs (heading hierarchy violations, broken relative links, duplicate headings, unclosed formatting).\n\nScope boundary: passive voice detection and the broader 'extended doctor rules' set live in FEAT-036 and do not ship here. This item wires existing rules end-to-end.",
      "priority": "must",
      "category": "editor",
      "steps": [
        "Add #[tauri::command] fn document_diagnose(state, window) -> Vec<Diagnostic> in src-tauri/src/lib.rs",
        "Define the Diagnostic struct (rule_id, severity, range, message) in markdown-core and re-export",
        "Add runDoctor() TypeScript wrapper in src/ that invokes document_diagnose with debounce",
        "Create a CodeMirror gutter extension that renders a marker per diagnostic line",
        "Create a Decoration.mark extension that underlines the offending range",
        "Add hover tooltip showing the diagnostic message",
        "Wire the doctor extension into author mode (not read mode)",
        "Verify diagnostics appear within 1s of a triggering edit and clear when the issue is fixed"
      ],
      "acceptanceCriteria": [
        {
          "text": "document_diagnose Tauri command exists",
          "verify": "grep -q 'fn document_diagnose' src-tauri/src/lib.rs"
        },
        {
          "text": "Doctor gutter extension exists and is registered",
          "verify": "grep -rE 'doctorGutter|doctorExtension' src/"
        },
        {
          "text": "Diagnostic type is defined in markdown-core",
          "verify": "grep -rE 'struct Diagnostic|pub struct Diagnostic' markdown-core/src/"
        },
        "Opening a document with a skipped heading level (h1 → h3) shows a gutter marker on the offending line within 1s",
        "Hovering the marker shows the rule message",
        "Fixing the issue clears the marker within 1s",
        "Diagnostics do not appear in read mode",
        "The 500ms debounce does not block typing (keystroke latency stays within baseline)"
      ],
      "scope": [
        "markdown-core/src/doctor.rs",
        "markdown-core/src/lib.rs",
        "src-tauri/src/lib.rs",
        "src/editor.ts",
        "src/style.css"
      ],
      "dependsOn": [
        "FEAT-045"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "Doctor engine already exists in Rust — this is wiring, not algorithm work. Measure keystroke latency against baseline before and after; if the doctor run regresses typing, it needs to go on a worker (not the per-keystroke path). Extended rules (passive voice, etc.) remain in FEAT-036.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "FEAT-051",
      "title": "Fix auto-save debounce drift — 2s → 1s to match spec",
      "intent": "ARCHITECTURE §3.7 specifies a 1-second auto-save debounce. FEAT-025 shipped 2s. This is a spec/code drift with no rationale documented. Pick one — the 1s target was chosen for 'saving feels immediate' (D-PERF-1), so match the spec.",
      "description": "Change the auto-save debounce from 2000ms to 1000ms in the editor. Verify the content-hash skip still prevents write amplification. If 1s turns out to cause battery or I/O issues on any platform, update ARCHITECTURE.md §3.7 with the real number rather than leaving a silent drift.",
      "priority": "must",
      "category": "editor",
      "steps": [
        "Find the auto-save debounce constant (likely in src/main.ts or src/editor.ts)",
        "Change 2000 to 1000",
        "Verify content-hash comparison still skips no-op saves on repeated trivial edits",
        "Smoke-test on a large document that auto-save does not cause keystroke lag"
      ],
      "acceptanceCriteria": [
        {
          "text": "Auto-save debounce is 1000ms",
          "verify": "grep -E 'scheduleAutoSave|AUTO_?SAVE' src/*.ts | grep -E '1000|1_000'"
        },
        "Typing a character and pausing 1.2s writes the file; typing again within 1s of a write does not write twice",
        "Large-document baseline keystroke latency stays within 110% of committed median"
      ],
      "scope": [
        "src/main.ts",
        "src/editor.ts"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "Trivial. One-line change + verification. Keep FEAT-025's hash-skip intact.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "FEAT-052",
      "title": "Planned IPC commands — document_format, document_undo, document_redo, document_viewport, document_close",
      "intent": "ARCHITECTURE §3.8 lists these as planned and names them as the mechanism the frontend uses to reach formatter, undo, and viewport functionality in the Rust core. Today the frontend uses CodeMirror's built-in history for undo and has no way to trigger a full-document reformat from the core. Without these commands the core formatter and undo coalescing (§3.5) are unreachable from the UI.",
      "description": "Add five Tauri commands to src-tauri/src/lib.rs, each backed by the corresponding core engine operation: document_format returns a list of mutations that fully reformats the document per the rule set in markdown-core/src/formatter.rs; document_undo and document_redo operate on the core's command history; document_viewport returns tokenized ranges for a byte-range viewport (sets up lazy tokenization for large files later); document_close releases per-window document state.\n\nScope boundary: document_diagnose is FEAT-050, not this item. Wiring document_format to a 'Format Document' menu item is in scope; wiring document_undo/redo to replace CodeMirror's history is a follow-up because it requires editor-side state reconciliation.",
      "priority": "should",
      "category": "core",
      "steps": [
        "Add #[tauri::command] fn document_format(state, window) -> Vec<Mutation> in src-tauri/src/lib.rs",
        "Define Mutation struct in markdown-core (byte range + replacement text)",
        "Add document_undo, document_redo, document_viewport, document_close commands",
        "Wire document_format to a 'Format Document' entry in the platform menu and a Shift+Alt+F shortcut",
        "Verify the formatter applies all five current rules (list continuation, table alignment, heading spacing, blank line sep, trailing whitespace trim)",
        "Add a unit test per command in markdown-core"
      ],
      "acceptanceCriteria": [
        {
          "text": "All five commands registered",
          "verify": "grep -cE 'fn document_(format|undo|redo|viewport|close)' src-tauri/src/lib.rs"
        },
        {
          "text": "Mutation struct defined",
          "verify": "grep -rE 'struct Mutation|pub struct Mutation' markdown-core/src/"
        },
        {
          "text": "Format Document menu entry wired",
          "verify": "grep -E 'Format Document|document_format' src-tauri/src/lib.rs src/*.ts"
        },
        "Running Format Document on a document with misaligned table pipes produces aligned output",
        "document_viewport returns tokens for the requested byte range"
      ],
      "scope": [
        "src-tauri/src/lib.rs",
        "markdown-core/src/lib.rs",
        "markdown-core/src/formatter.rs",
        "src/editor.ts",
        "src/markdown-commands.ts"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "document_undo/redo replacing CodeMirror history is deliberately out of scope — only expose the commands; the editor keeps using history() for now. Revisit when undo coalescing (§3.5) becomes a user-visible need.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "FEAT-053",
      "title": "Hot-path formatting rules in editor with parity test against core",
      "intent": "ARCHITECTURE A-CORE-3 specifies that formatting rules which must respond within a single frame (Enter on a list, Tab in a list, table alignment on pipe typing) are duplicated in the editor frontend for zero-latency response, and that tests enforce identical output between the two implementations. Today the frontend relies on @codemirror/lang-markdown for basic list continuation; table alignment and heading spacing are not handled editor-side. This is the mechanism D-PERF-1 ('edits feel instant') relies on.",
      "description": "Port the five formatter rules from markdown-core/src/formatter.rs into the editor in TypeScript, wired as CodeMirror transactions that fire on the triggering keystroke (Enter on list item, pipe in a table, etc.). Add a parity test suite that runs the same input corpus through both the TypeScript and Rust implementations and asserts identical output — this is the guardrail that keeps the duplication from drifting.\n\nScope boundary: this item is about the five shipped rules only. Any rule added to the core formatter after this ships must also land in the editor in the same item.",
      "priority": "should",
      "category": "editor",
      "steps": [
        "Port each rule (list continuation, table alignment, heading spacing, blank line sep, trailing whitespace trim) into src/markdown-commands.ts or a new src/formatter.ts",
        "Wire each rule to its triggering keystroke via CodeMirror transactionFilter or keymap",
        "Create a parity test: shared corpus of (input, expected) pairs; run through both implementations in CI",
        "Update ARCHITECTURE.md §3.3 if any rule intentionally diverges editor-side (none expected)"
      ],
      "acceptanceCriteria": [
        {
          "text": "Editor-side formatter module exists",
          "verify": "test -f src/formatter.ts || grep -q 'tableAlign\\|headingSpacing' src/markdown-commands.ts"
        },
        {
          "text": "Parity test exists and runs in CI",
          "verify": "grep -rE 'formatter.*parity|parity.*formatter' tests/ markdown-core/tests/"
        },
        "Typing a pipe at end of table row auto-aligns all pipes in the table within one frame",
        "Pressing Enter on a numbered list auto-continues the numbering",
        "Pressing Enter on an empty list item exits the list",
        "Parity test asserts zero diffs across the committed corpus"
      ],
      "scope": [
        "src/formatter.ts",
        "src/markdown-commands.ts",
        "src/editor.ts",
        "tests/",
        "markdown-core/tests/"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "FEAT-053 partial landed in 726e6af — markdownKeymap enabled for list continuation on Enter (most common hot-path keystroke). Remaining scope: port Rust table-alignment and heading-spacing rules to TypeScript, add a parity test harness with a committed input/output corpus. Table alignment while typing pipes is the next user-visible improvement.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "FEAT-054",
      "title": "UTF-8 BOM and non-UTF-8 encoding detection on open",
      "intent": "ARCHITECTURE §3.7 specifies UTF-8 as the default assumption, BOM detection for UTF-8-with-BOM files, and flagging of non-UTF-8 files to the user rather than silently mangling them. Line-ending preservation (D-FILE-3) is implemented; encoding preservation is not visibly implemented and tested. If a user opens a Windows-1252 or UTF-16 file we need to behave predictably.",
      "description": "In markdown-core's file load path, detect the BOM (UTF-8, UTF-16-LE, UTF-16-BE). For UTF-8-with-BOM, strip the BOM on load and re-emit it on save. For UTF-16 or non-UTF-8 content, return an error to the Tauri shell which surfaces a non-modal banner offering to reopen with a specified encoding or cancel — not silent replacement, not silent mangling.\n\nScope boundary: this item ships BOM round-trip for UTF-8 and clear failure for non-UTF-8. Actual conversion/reopen with chosen encoding is a follow-up if users request it.",
      "priority": "should",
      "category": "core",
      "steps": [
        "Add encoding detection in markdown-core file open path: check first 3 bytes for UTF-8 BOM (EF BB BF), 2 bytes for UTF-16 BOMs",
        "For UTF-8 BOM: strip on load, remember flag, re-emit BOM on save",
        "For UTF-16 or invalid UTF-8: return EncodingError with detected encoding name",
        "Surface EncodingError in the shell as a non-modal error banner (ties into D-ERR-1)",
        "Write round-trip tests for UTF-8 and UTF-8-with-BOM",
        "Write a test that non-UTF-8 input returns the expected error"
      ],
      "acceptanceCriteria": [
        {
          "text": "BOM handling path exists in markdown-core",
          "verify": "grep -rE 'BOM|bom|\\\\u{feff}' markdown-core/src/"
        },
        {
          "text": "Encoding error surfaces to the shell",
          "verify": "grep -rE 'EncodingError|encoding_error' markdown-core/src/ src-tauri/src/"
        },
        "Opening a UTF-8-with-BOM file, editing, and saving produces a file whose first three bytes are still EF BB BF",
        "Opening a UTF-8 file (no BOM), editing, and saving does not add a BOM",
        "Opening a UTF-16-LE file surfaces a non-modal error banner; no silent mangling"
      ],
      "scope": [
        "markdown-core/src/lib.rs",
        "src-tauri/src/lib.rs",
        "src/main.ts"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": true,
      "groomedAt": "2026-04-21",
      "groomingNotes": "Do not pull in a charset detection crate for this — BOM-only detection is all §3.7 requires, and adding chardet-like inference invites silent misclassification. Explicit error for non-UTF-8 is the correct behavior.",
      "phase": "0",
      "epic": null
    },
    {
      "id": "EPIC-CORE-API",
      "title": "Phase0-core-api: Extract the transport-agnostic command surface into markdown-core",
      "intent": "ARCHITECTURE §3.8 requires a single command surface in the core reached by uniffi (Apple) and WASM (web) — 'no separate API per platform, only a separate binding.' Today that orchestration lives inside src-tauri/src/lib.rs, so neither real binding can exist. This extraction is the root of the whole build plan's critical path.",
      "description": "Relocate the orchestration in src-tauri/src/lib.rs (doctor diagnose, format mutations, viewport, undo/redo, wikilink resolve/create, backlinks, watching lifecycle, content-level file I/O) into markdown-core as plain Rust functions over a Session/Workspace type. RELOCATE, don't rewrite — the algorithms already work. Tauri commands become thin pass-throughs that call the core (temporary, removed in EPIC-RETIRE-TAURI). Platform glue (keychain, OS dialogs, recent-files persistence, AI HTTP) stays OUT of the core. Set crate-type=['rlib','cdylib','staticlib']. Fill docs/CORE-API.md as the contract both bindings implement.\n\nScope boundary: this item moves logic and proves the core loop through a direct core API call; it does not add the WASM or uniffi binding (EPIC-WASM / EPIC-UNIFFI).",
      "priority": "must",
      "category": "core",
      "phase": "0",
      "epic": "EPIC-CORE-API",
      "steps": [
        "Define a Session/Workspace type in markdown-core that owns document + watch + history state",
        "Move resolve_wikilink, compute_backlinks, diagnose, format, viewport, undo/redo, watch lifecycle from src-tauri/src/lib.rs into markdown-core as public functions",
        "Leave the Tauri commands as thin wrappers calling the new core functions (temporary)",
        "Set crate-type = ['rlib','cdylib','staticlib'] in markdown-core/Cargo.toml",
        "Write a core-level integration test: open -> edit -> save_buffer -> reopen via the core API (no Tauri)",
        "Fill in docs/CORE-API.md with the finalized operation signatures"
      ],
      "acceptanceCriteria": [
        {
          "text": "markdown-core declares cdylib + staticlib crate types",
          "verify": "grep -A2 'crate-type' markdown-core/Cargo.toml | grep -q cdylib && grep -A2 'crate-type' markdown-core/Cargo.toml | grep -q staticlib"
        },
        {
          "text": "Session/Workspace type exists in the core",
          "verify": "grep -rqE 'struct (Session|Workspace)' markdown-core/src/"
        },
        {
          "text": "Doctor/format/wikilink orchestration now lives in the core, not only the shell",
          "verify": "grep -rqE 'fn (diagnose|format_document|resolve_wikilink)' markdown-core/src/"
        },
        {
          "text": "Core round-trip test passes without Tauri",
          "verify": "cargo test -p markdown-core --quiet 2>&1 | tail -3"
        },
        "Opening, editing, saving and reopening a document through the core API (no IPC) round-trips byte-for-byte on the baseline corpus",
        "src-tauri commands contain no business logic — only pass-through calls into markdown-core"
      ],
      "scope": [
        "markdown-core/Cargo.toml",
        "markdown-core/src/lib.rs",
        "src-tauri/src/lib.rs",
        "docs/CORE-API.md"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Linchpin of the build plan. The win condition is 'no business logic left in the shell.' Do not change behavior — this is a move + test, measured against baseline before/after."
    },
    {
      "id": "EPIC-WASM",
      "title": "Phase1-wasm: Compile markdown-core to WebAssembly and bind it to the web frontend",
      "intent": "ARCHITECTURE §8 flags 'core-to-WASM viability' as a live risk — tree-sitter's C runtime must compile to WASM. The entire web/Windows/Linux/Android delivery depends on the core running in the browser. This item proves it end-to-end with a real browser harness before any feature port.",
      "description": "Add wasm-bindgen/wasm-pack targeting wasm32-unknown-unknown. Spike the tree-sitter-md-in-WASM compile FIRST and fail fast. Expose the Phase-0 command surface as a JS API (buffer-in/buffer-out; the shell owns file handles). Build a deliberately-minimal HTML harness that drives every entry point against docs/baseline-corpus. Capture an in-browser web baseline slice and extend the regression gate to it.\n\nScope boundary: this item is the binding + skeleton, not the production cutover (EPIC-CUTOVER).",
      "priority": "must",
      "category": "core",
      "phase": "1",
      "epic": "EPIC-WASM",
      "steps": [
        "Spike: confirm markdown-core (incl. tree-sitter-md) compiles to wasm32-unknown-unknown; fail fast if not",
        "Add wasm-bindgen + a wasm-pack/build step producing a loadable module",
        "Expose the CORE-API operations as JS-callable functions (open/edit/save_buffer/format/diagnose/viewport/undo/redo/resolve_wikilink)",
        "Build a throwaway HTML harness that runs the full loop against the baseline corpus using the real WASM core",
        "Capture in-browser per-slice medians and add a web profile to docs/baseline.json",
        "Document the tree-sitter-in-WASM outcome in docs/large-file-performance.md or ARCHITECTURE §8"
      ],
      "acceptanceCriteria": [
        {
          "text": "markdown-core builds for wasm32",
          "verify": "cargo build -p markdown-core --target wasm32-unknown-unknown 2>&1 | tail -3"
        },
        {
          "text": "A WASM build artifact + JS bindings are produced",
          "verify": "find . -path ./target -prune -o -name '*.wasm' -print -o -name 'markdown_core*.js' -print | grep -q ."
        },
        "Loading the harness in a real browser opens, edits, formats, diagnoses, saves-buffer and reopens a corpus document through the WASM core (no marked, no stub)",
        "tree-sitter parsing demonstrably runs in WASM (parse a corpus doc and assert the AST in-browser)",
        "The web baseline slice is committed and the gate fails on an injected in-browser regression"
      ],
      "scope": [
        "markdown-core/Cargo.toml",
        "markdown-core/src/lib.rs",
        "src/",
        "docs/baseline.json",
        "package.json"
      ],
      "dependsOn": [
        "EPIC-CORE-API"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Lead with the parser-in-WASM spike — it is the single highest-impact unknown. Walking-skeleton ACs: prove it in a real browser, not with file-existence checks. SPIKE RESOLVED 2026-06-19 (docs/wasm-spike.md): PROVEN — markdown-core (incl. tree-sitter) compiles to wasm32-wasip1 and RUNS under wasmtime over the full baseline corpus (scripts/build-wasm.sh run; bin markdown-core/src/bin/wasm_smoke.rs). Toolchain chosen: wasm32-wasip1 + wasi-libc sysroot + llvm clang. Remaining EPIC-WASM scope: replace the WASI argv/stdout smoke with extern-C buffer-in/out entry points (no wasm-bindgen under WASI), instantiate in-browser via a WASI shim with the PWA owning the file handle, and capture an in-browser web baseline slice. crate-types + notify-off-wasm32 landed in EPIC-CORE-API."
    },
    {
      "id": "EPIC-UNIFFI",
      "title": "Phase1prime-uniffi: Bind markdown-core to Swift via uniffi as an XCFramework",
      "intent": "The native Apple frontend (the lead platform per PRODUCT D-ROAD-1) cannot begin until Swift can call the core in-process. ARCHITECTURE §3.8 / A-STACK-2 specify uniffi over a static library. This item produces that binding and proves the FFI boundary before any UI.",
      "description": "Add uniffi to markdown-core (or a thin markdown-core-ffi wrapper crate). Define the binding surface mirroring CORE-API. Build staticlib for aarch64-apple-ios and aarch64-apple-darwin, generate Swift bindings, and package an XCFramework. A Swift unit test must round-trip open -> edit -> save_buffer -> reopen through uniffi.\n\nScope boundary: binding + FFI proof only; the TextKit 2 editor is EPIC-APPLE-SKELETON. Requires a macOS CI runner.",
      "priority": "must",
      "category": "core",
      "phase": "1prime",
      "epic": "EPIC-UNIFFI",
      "steps": [
        "Add uniffi scaffolding (UDL or proc-macro) over the CORE-API surface",
        "Build staticlib for aarch64-apple-ios and aarch64-apple-darwin",
        "Generate Swift bindings and package an XCFramework",
        "Write a Swift unit test linking the static lib that round-trips the core loop via uniffi",
        "Add a macOS CI job that builds the XCFramework and runs the Swift test"
      ],
      "acceptanceCriteria": [
        {
          "text": "uniffi is a dependency of the core (or ffi wrapper)",
          "verify": "grep -rq uniffi markdown-core/Cargo.toml */Cargo.toml 2>/dev/null"
        },
        {
          "text": "Apple static-lib targets build",
          "verify": "cargo build -p markdown-core --target aarch64-apple-darwin 2>&1 | tail -3"
        },
        "An XCFramework is produced and consumable from a Swift package",
        "A Swift test opens, edits, saves and reopens a document through uniffi and asserts a byte-for-byte round-trip"
      ],
      "scope": [
        "markdown-core/Cargo.toml",
        "markdown-core/src/lib.rs",
        ".github/workflows/build.yml"
      ],
      "dependsOn": [
        "EPIC-CORE-API"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Parallel to EPIC-WASM; both target the same CORE-API surface so they cannot drift. Needs Apple toolchain on CI — gate the job on macos-latest."
    },
    {
      "id": "EPIC-CUTOVER",
      "title": "Phase2-cutover: Replace Tauri IPC with the WASM core + PWA shell at parity",
      "intent": "D-PLAT-1 + A-STACK-2: the web frontend must reach the core via WASM and the browser's own file APIs, not Tauri IPC. src/main.ts has ~15 invoke() sites and a separate degraded main-web.ts that bypasses the core. This item converges them onto one Tauri-free PWA at feature parity with today's experience.",
      "description": "Introduce one `core` adapter (the EPIC-WASM API) and replace every invoke() site in src/main.ts and the Tauri-reaching imports in doctor.ts/settings.ts/wikilinks.ts/format.ts/ai.ts with WASM-core or PWA-shell calls. Converge the two web entries: promote main.ts's full feature set onto the WASM backend, fold in main-web.ts's File System Access logic, delete the degraded marked path and tauri-stub.ts. Build the PWA shell: File System Access open/save (+ input/download fallback), recent files (IndexedDB), file-change/conflict detection (replacing start_watching), auto-save (1s, FEAT-051), service worker offline + install manifest.\n\nScope boundary: removing src-tauri and re-pointing CI are EPIC-RETIRE-TAURI / EPIC-CI-REPOINT.",
      "priority": "must",
      "category": "editor",
      "phase": "2",
      "epic": "EPIC-CUTOVER",
      "steps": [
        "Create a single src/core.ts adapter wrapping the WASM core API",
        "Replace every invoke() call in src/main.ts with a core.ts or PWA-shell call",
        "Fold main-web.ts's File System Access open/save/save-as + fallbacks into the unified entry; remove the marked-based preview path",
        "Implement recent files via IndexedDB and external-change/conflict detection without Tauri",
        "Delete src/tauri-stub.ts once nothing imports @tauri-apps",
        "Verify the PWA installs, works offline, and reaches feature parity (read/author modes, doctor gutter, format, wikilinks, find/replace, word count)"
      ],
      "acceptanceCriteria": [
        {
          "text": "No invoke() calls remain in src/",
          "verify": "! grep -rqE '\\binvoke\\(' src/"
        },
        {
          "text": "No @tauri-apps imports remain in src/",
          "verify": "! grep -rq '@tauri-apps' src/"
        },
        {
          "text": "tauri-stub.ts is deleted",
          "verify": "! test -f src/tauri-stub.ts"
        },
        {
          "text": "Web build succeeds",
          "verify": "npm run build:web 2>&1 | tail -3"
        },
        "The installed PWA opens, edits, saves and reopens a real file via File System Access, offline-capable",
        "Doctor gutter, format, wikilinks, find/replace, word count and read/author modes all work against the WASM core at parity with the prior Tauri build"
      ],
      "scope": [
        "src/core.ts",
        "src/main.ts",
        "src/main-web.ts",
        "src/tauri-stub.ts",
        "src/sw.ts",
        "package.json"
      ],
      "dependsOn": [
        "EPIC-WASM"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Do NOT delete Tauri until this reaches parity — the invoke() call-site list is the explicit checklist of what must be replaced. Convergence of the two entries is the heart of the item."
    },
    {
      "id": "EPIC-RETIRE-TAURI",
      "title": "Phase2-retire-tauri: Remove the Tauri shell, deps, and glib patch",
      "intent": "Once the PWA reaches parity (EPIC-CUTOVER), the Tauri shell is dead weight: it constrains the workspace (the glib RUSTSEC patch exists only for Tauri's gtk) and contradicts A-STACK-1 ('no cross-platform UI framework'). Remove it cleanly.",
      "description": "Remove the src-tauri/ workspace member, the glib [patch.crates-io] in the root Cargo.toml (it exists solely for Tauri/gtk), the @tauri-apps/* npm deps, and the 'tauri' npm script. The workspace must build with markdown-core alone.\n\nScope boundary: CI re-point is EPIC-CI-REPOINT (can land in the same PR).",
      "priority": "must",
      "category": "core",
      "phase": "2",
      "epic": "EPIC-RETIRE-TAURI",
      "steps": [
        "Remove 'src-tauri' from the workspace members in the root Cargo.toml",
        "Delete the src-tauri/ directory",
        "Remove the glib [patch.crates-io] block from the root Cargo.toml",
        "Remove @tauri-apps/* dependencies and the 'tauri' script from package.json",
        "Run cargo build --workspace and npm run build:web to confirm nothing depended on the removed pieces"
      ],
      "acceptanceCriteria": [
        {
          "text": "src-tauri is no longer a workspace member",
          "verify": "! grep -q 'src-tauri' Cargo.toml"
        },
        {
          "text": "The glib patch is gone",
          "verify": "! grep -q 'patch.crates-io' Cargo.toml"
        },
        {
          "text": "No @tauri-apps deps remain",
          "verify": "! grep -q '@tauri-apps' package.json"
        },
        {
          "text": "Workspace builds without Tauri",
          "verify": "cargo build --workspace 2>&1 | tail -3"
        }
      ],
      "scope": [
        "Cargo.toml",
        "src-tauri/",
        "package.json"
      ],
      "dependsOn": [
        "EPIC-CUTOVER"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Gated on EPIC-CUTOVER parity. Removing the glib patch is a bonus payoff — it only existed to work around Tauri's gtk soundness bug."
    },
    {
      "id": "EPIC-CI-REPOINT",
      "title": "Phase2-ci-repoint: Re-point CI off Tauri to PWA + Apple builds",
      "intent": "FEAT-048 (done) built a Tauri-centric matrix (.app/MSI/WebKitGTK). After the cutover those jobs build a retired stack. CI must build the PWA as the primary web artifact and add the Apple uniffi build, while keeping the baseline regression gate.",
      "description": "Rewrite .github/workflows/build.yml: drop the Tauri CLI install, the .app and MSI bundle jobs, and the WebKitGTK system-dep installs. Make build-web (vite build --config vite.config.web.ts) the primary build and artifact. Add an Apple job (macos-latest) that builds the XCFramework and runs the Swift uniffi test. Keep cargo build/test, the CommonMark suite, and scripts/measure_baseline.sh --check.\n\nScope boundary: supersedes the Tauri parts of FEAT-048.",
      "priority": "should",
      "category": "ci",
      "phase": "2",
      "epic": "EPIC-CI-REPOINT",
      "steps": [
        "Remove Tauri CLI install, .app/MSI bundle steps, and WebKitGTK apt installs from build.yml",
        "Make build-web the primary build and upload dist-web as the release artifact",
        "Add a macos build job that builds the XCFramework and runs the Swift uniffi round-trip test",
        "Keep cargo build/test --workspace, the CommonMark spec suite, and measure_baseline.sh --check",
        "Confirm the workflow is valid YAML and goes green on a test PR"
      ],
      "acceptanceCriteria": [
        {
          "text": "No Tauri references remain in build.yml",
          "verify": "! grep -qiE 'tauri|webkit2gtk|\\.msi' .github/workflows/build.yml"
        },
        {
          "text": "build-web is present",
          "verify": "grep -q 'build-web' .github/workflows/build.yml"
        },
        {
          "text": "An Apple/macos build job is present",
          "verify": "grep -qiE 'macos|xcframework|uniffi' .github/workflows/build.yml"
        },
        {
          "text": "build.yml is valid YAML",
          "verify": "python3 -c 'import yaml; yaml.safe_load(open(\".github/workflows/build.yml\"))'"
        },
        {
          "text": "Baseline gate is retained",
          "verify": "grep -q 'measure_baseline.sh' .github/workflows/build.yml"
        }
      ],
      "scope": [
        ".github/workflows/build.yml"
      ],
      "dependsOn": [
        "EPIC-RETIRE-TAURI"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Supersedes FEAT-048's Tauri jobs. The baseline --check job must survive the rewrite."
    },
    {
      "id": "EPIC-APPLE-SKELETON",
      "title": "Phase2prime-apple-skeleton: iOS TextKit 2 walking skeleton over the uniffi core",
      "intent": "PRODUCT D-ROAD-1 makes iOS the lead platform and the native-feel bar. ARCHITECTURE §8 flags 'native Apple editor effort' as a real risk to validate by use, not by compile. This item proves the native frontend binds the core via uniffi and edits real text in TextKit 2 on a real device — deliberately minimal.",
      "description": "Create an Xcode/SwiftPM project consuming the EPIC-UNIFFI XCFramework, iOS target first. Build a minimal TextKit 2 editor view bound to the core; wire Files-app open/save; prove the open -> edit -> save -> reopen loop on a real device. Capture an Apple baseline slice (open/keystroke/save medians) on device. No WYSIWYM polish yet.\n\nScope boundary: read mode, WYSIWYM, share sheet, accessibility, and macOS are EPIC-APPLE-FEATURES.",
      "priority": "must",
      "category": "shell",
      "phase": "2prime",
      "epic": "EPIC-APPLE-SKELETON",
      "steps": [
        "Create the Xcode/SwiftPM project consuming the XCFramework; iOS target",
        "Mount a minimal TextKit 2 (UITextView) editor bound to the core via uniffi",
        "Wire Files-app open and save (UIDocument / document browser)",
        "Prove open -> edit -> save -> reopen on a real device",
        "Capture an Apple baseline slice on device and commit it"
      ],
      "acceptanceCriteria": [
        {
          "text": "An Xcode/SwiftPM project exists",
          "verify": "find . -name '*.xcodeproj' -o -name 'Package.swift' -path '*pple*' | grep -q ."
        },
        "The iOS app launches on a real device and opens a .md file from Files",
        "Editing in the TextKit 2 view and saving writes back to the original file; reopening round-trips",
        "An Apple baseline slice (on-device medians) is committed to docs/baseline.json"
      ],
      "scope": [
        "apple/",
        "docs/baseline.json"
      ],
      "dependsOn": [
        "EPIC-UNIFFI"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Walking skeleton on a REAL device (A-PROC-3) — the bar is validated by use. Manual on-device ACs are expected and correct here."
    },
    {
      "id": "EPIC-PWA-RELEASE",
      "title": "Phase3-pwa-release: Harden the PWA to a Web/Windows/Linux/Android public pre-release",
      "intent": "PRODUCT §5 makes the web build the first public pre-release and the delivery vehicle for Windows, Linux, and Android (no native shells). This item hardens the post-cutover PWA to public quality across browsers.",
      "description": "Harden PWA install/UX on Chromium (Windows/Linux/Android) and graceful degradation on Firefox/Safari (no File System Access -> input/download fallback). Finalize conflict-detection UX. Cross-browser parity QA against the web baseline. Distribution is static host + PWA install — no Flatpak/MSI/WinGet.\n\nScope boundary: feature work (rich content) is Phase 4; this item is hardening + release of what exists.",
      "priority": "should",
      "category": "editor",
      "phase": "3",
      "epic": "EPIC-PWA-RELEASE",
      "steps": [
        "Polish the install manifest, icons, and offline service-worker behavior",
        "Verify graceful fallback on Firefox/Safari where File System Access is unavailable",
        "Finalize the external-change / conflict-resolution UX",
        "Cross-browser parity QA (Chromium desktop + Android, Firefox, Safari) against the web baseline",
        "Document the install paths for Windows/Linux/Android (PWA install, not Flatpak/MSI)"
      ],
      "acceptanceCriteria": [
        {
          "text": "A web app manifest is present",
          "verify": "find . -path ./node_modules -prune -o -name 'manifest*.json' -print -o -name 'manifest.webmanifest' -print | grep -q ."
        },
        {
          "text": "Web build succeeds",
          "verify": "npm run build:web 2>&1 | tail -3"
        },
        "The PWA installs and runs offline on Chromium desktop and Android",
        "On Firefox/Safari the editor works with the input/download file fallback",
        "Cross-browser QA passes and the web baseline slice stays within the 1.1x gate"
      ],
      "scope": [
        "src/",
        "public/",
        "vite.config.web.ts"
      ],
      "dependsOn": [
        "EPIC-RETIRE-TAURI"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "No Flatpak/MSI/WinGet — those died with Tauri. Distribution is the PWA itself."
    },
    {
      "id": "EPIC-APPLE-FEATURES",
      "title": "Phase3prime-apple-features: iOS+macOS feature parity on TextKit 2",
      "intent": "DP-2/DP-4: the Apple frontend (the quality bar) must reach the same read/write feature set as the web frontend, then share the TextKit 2 surface to macOS. This is where the native-feel investment pays off.",
      "description": "Bring the Apple frontend to product parity: read mode, WYSIWYM on TextKit 2, mode transition, native file management, share sheet, accessibility (UIAccessibility/NSAccessibility), and hot-path formatting mirrored from the core (parity-tested via FEAT-053). Then add the macOS target sharing the NSTextView surface. Each lands against the Apple baseline.\n\nScope boundary: rich content (Mermaid/math/PDF) is Phase 4; AI is post-v1.0.",
      "priority": "should",
      "category": "shell",
      "phase": "3prime",
      "epic": "EPIC-APPLE-FEATURES",
      "steps": [
        "Implement read mode and WYSIWYM rendering on TextKit 2",
        "Implement the read<->author mode transition",
        "Add native file management, share sheet, and accessibility",
        "Mirror hot-path formatting rules from the core and gate them with the FEAT-053 parity harness",
        "Add the macOS target sharing the NSTextView surface",
        "Maintain the Apple baseline slice across each addition"
      ],
      "acceptanceCriteria": [
        {
          "text": "Apple frontend sources exist beyond the skeleton",
          "verify": "find . -path '*pple*' -name '*.swift' | head -1 | grep -q ."
        },
        "iOS reaches read mode + WYSIWYM + mode transition + share sheet + accessibility",
        "macOS runs the same editor surface (NSTextView) as iOS",
        "Hot-path formatting parity tests are green for the Apple implementation",
        "The Apple baseline slice stays within the 1.1x gate across the feature work"
      ],
      "scope": [
        "apple/",
        "markdown-core/tests/"
      ],
      "dependsOn": [
        "EPIC-APPLE-SKELETON"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-06-19",
      "groomingNotes": "Parity test (FEAT-053) is the guardrail that keeps the Apple hot-path rules identical to the core."
    }
  ]
}
