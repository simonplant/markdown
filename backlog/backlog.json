{
  "description": "Feature backlog",
  "items": [
    {
      "id": "FEAT-001",
      "title": "M0-workspace: Rust workspace and em-core crate",
      "intent": "A developer can run cargo test -p em-core and watch a real file round-trip through open, edit, save, and read back — no mocks, no stubs, no in-memory fake filesystem. This is the foundation everything else builds on.",
      "description": "Scaffold the Rust workspace and the em-core crate that all other components depend on. em-core owns the document model at this stage: a plain String-backed struct (NOT a piece table — that comes in Phase 0 post-M0). Expose exactly four public functions: open_file(path) -> Document, edit(doc, offset, delete, insert) -> (), save_file(doc, path), current_text(doc) -> &str. Write an integration test that (1) writes a temp file with known content, (2) opens it, (3) applies an edit, (4) saves, (5) reopens and asserts the edited content is present. No parser, no formatter, no diagnostics — explicitly out of scope for this item per PRODUCT.md §7.1.3.",
      "priority": "must",
      "category": "scaffolding",
      "steps": [
        "Run cargo init --name em-core in the em-core/ directory to create the library crate",
        "Create a Cargo.toml workspace root with members = [\"em-core\"]",
        "Define a Document struct in em-core/src/lib.rs holding a single String field",
        "Implement open_file(path: &str) -> Result<Document, io::Error> using std::fs::read_to_string",
        "Implement edit(&mut self, offset: usize, delete: usize, insert: &str) on Document — naive String splice for now",
        "Implement save_file(&self, path: &str) -> Result<(), io::Error> using std::fs::write",
        "Implement current_text(&self) -> &str returning the inner String slice",
        "Write tests/integration_test.rs: create tmpfile with known content, open, apply an edit, save, reopen, assert edited content is present",
        "Confirm cargo test -p em-core passes with zero failures"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test -p em-core passes with at least one integration test that opens a real file, edits it, saves it, and reads it back",
          "verify": "cargo test -p em-core 2>&1 | grep -qE 'test result: ok'"
        },
        {
          "text": "cargo build --workspace completes with zero errors",
          "verify": "cargo build --workspace 2>&1 | grep -qcv 'error\\[' || cargo build --workspace"
        },
        {
          "text": "The four public API functions (open_file, edit, save_file, current_text) exist and are callable from outside the crate",
          "verify": "cargo test -p em-core 2>&1 | grep -qE 'test result: ok'"
        }
      ],
      "scope": [
        "em-core/**",
        "Cargo.toml",
        "Cargo.lock"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "FEAT-002",
      "title": "M0-tauri-shell: Tauri 2.0 macOS app scaffold",
      "intent": "cargo tauri build produces a real .app bundle on macOS that opens a window with a webview. The build must succeed in CI without manual intervention. em-core is wired as a direct Rust dependency of the Tauri backend — no separate process, no mocks.",
      "description": "Scaffold a Tauri 2.0 project targeting macOS. The Tauri backend (src-tauri/) must depend on em-core from the workspace. The webview loads an HTML page (can be a blank page with a placeholder — the editor comes in M0-editor). The app must produce a working window that opens on launch. Wire CI (GitHub Actions) to build the Tauri app on macOS and verify the build artifact exists. This item does NOT include the editor, IPC bridge, or file operations — those come in subsequent M0 items.",
      "priority": "must",
      "category": "scaffolding",
      "steps": [
        "Run npm create tauri-app@latest or cargo tauri init in the repo root to scaffold the Tauri 2.0 project",
        "Update the workspace Cargo.toml to include src-tauri as a member",
        "Add em-core as a path dependency in src-tauri/Cargo.toml: em-core = { path = '../em-core' }",
        "Verify the Tauri app compiles: cargo build -p em-markdown (or whatever the tauri crate is named)",
        "Create a minimal src/index.html (or equivalent) — a div with 'Easy Markdown' text is sufficient for M0",
        "Add a GitHub Actions workflow .github/workflows/build.yml that runs on push/PR, checks out code, installs Rust + Node deps, runs cargo tauri build on macOS runner",
        "Confirm the workflow passes and produces a .app artifact"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo build --workspace completes with src-tauri included, no errors",
          "verify": "cargo build --workspace 2>&1 | grep -qcv 'error\\[' || true"
        },
        {
          "text": "The GitHub Actions build workflow file exists and is valid YAML",
          "verify": "test -f .github/workflows/build.yml"
        },
        {
          "text": "src-tauri/Cargo.toml declares em-core as a dependency",
          "verify": "grep -q 'em-core' src-tauri/Cargo.toml"
        }
      ],
      "scope": [
        "src-tauri/**",
        "src/**",
        ".github/workflows/**",
        "Cargo.toml",
        "package.json"
      ],
      "dependsOn": [
        "FEAT-001"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-003",
      "title": "M0-bridge: Tauri IPC commands wiring em-core to the webview",
      "intent": "JavaScript in the webview can call invoke('open_file', {path}) and receive the file contents, call invoke('edit', ...) to mutate the document, and call invoke('save_file', ...) to persist it. Every bridge call goes through real em-core code — no in-memory stubs.",
      "description": "Expose the four em-core functions as Tauri IPC commands so the webview can call them. Each command must use the #[tauri::command] macro and be registered in the Tauri builder. The commands manage document state in the Tauri app state (AppState struct holding a Mutex<Option<Document>>). Write an integration test or end-to-end test that calls each command and verifies the result from the webview side. This item does NOT include the editor UI — only the IPC bridge and its test.",
      "priority": "must",
      "category": "scaffolding",
      "steps": [
        "Define AppState struct in src-tauri/src/main.rs with a Mutex<Option<em_core::Document>> field",
        "Register AppState with .manage(AppState::default()) in the Tauri builder",
        "Implement #[tauri::command] fn open_file(state, path) that calls em_core::open_file and stores the result in AppState",
        "Implement #[tauri::command] fn edit(state, offset, delete, insert) that calls Document::edit on the stored doc",
        "Implement #[tauri::command] fn save_file(state, path) that calls Document::save_file",
        "Implement #[tauri::command] fn current_text(state) -> String that returns the stored doc's current_text",
        "Register all four commands in .invoke_handler(tauri::generate_handler![...])",
        "Write a Tauri integration test (or webview-side JS test) that exercises each command against a temp file"
      ],
      "acceptanceCriteria": [
        {
          "text": "src-tauri/src/main.rs registers all four tauri commands (open_file, edit, save_file, current_text)",
          "verify": "grep -c 'tauri::command' src-tauri/src/main.rs | grep -qE '^[4-9]|^[1-9][0-9]'"
        },
        {
          "text": "cargo test --workspace passes all tests including bridge-level integration tests",
          "verify": "cargo test --workspace 2>&1 | grep -qE 'test result: ok'"
        },
        {
          "text": "The invoke_handler in main.rs includes all four command functions",
          "verify": "grep -q 'generate_handler' src-tauri/src/main.rs"
        }
      ],
      "scope": [
        "src-tauri/src/**",
        "em-core/**"
      ],
      "dependsOn": [
        "FEAT-002"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-004",
      "title": "M0-editor: CodeMirror 6 plain-text editor in the webview",
      "intent": "The webview displays a CodeMirror 6 editor instance. The user can type characters and see them appear immediately in the editor. No IPC yet — this item proves CodeMirror 6 is mounted, receives keystrokes, and maintains a local buffer.",
      "description": "Mount CodeMirror 6 in the Tauri webview with a minimal plain-text configuration. No markdown mode, no syntax highlighting, no decorations — raw text input only. The editor must be focusable, must accept keyboard input, and must maintain its content in a local JS buffer. Wire up a basic TypeScript/JS module that imports CodeMirror 6 packages via npm and initializes an EditorView. IPC bridge integration comes in M0-open-save-loop — this item ends when the editor is visible and accepts input.",
      "priority": "must",
      "category": "scaffolding",
      "steps": [
        "Add CodeMirror 6 packages as npm dependencies: @codemirror/view, @codemirror/state",
        "Create src/editor.ts (or .js) that imports EditorView and EditorState from @codemirror/view and @codemirror/state",
        "Initialize an EditorView with a minimal setup (basicSetup from codemirror package, or manual extensions list)",
        "Mount the EditorView into a DOM element on index.html load",
        "Verify the editor is visible in the webview and accepts keyboard input",
        "Export a getContent() function from editor.ts that returns the current editor string content",
        "Wire the build system (Vite or equivalent) to bundle the TypeScript/JS for the Tauri webview"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm install and npm run build complete without errors",
          "verify": "npm install && npm run build 2>&1 | grep -qcv 'ERROR'"
        },
        {
          "text": "The built output includes a JS bundle that contains CodeMirror references",
          "verify": "test -f dist/index.html || test -f src/index.html"
        },
        {
          "text": "src/editor.ts (or equivalent) imports from @codemirror packages and initializes an EditorView",
          "verify": "grep -r 'EditorView' src/ | grep -qv 'node_modules'"
        }
      ],
      "scope": [
        "src/**",
        "package.json",
        "vite.config.*",
        "tsconfig.*"
      ],
      "dependsOn": [
        "FEAT-002"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-005",
      "title": "M0-open-save-loop: End-to-end open, edit, save via IPC",
      "intent": "A user can launch the app, open a .md file via the native OS file dialog, type characters that appear instantly, press Cmd+S to save, and reopen the file to see their edits persisted. All nine steps from PRODUCT.md §7.1.1 must work with real files on a real filesystem. Silent failure at any step is unacceptable.",
      "description": "Wire the CodeMirror 6 editor to the Tauri IPC bridge so that (1) clicking Open invokes the native file dialog and loads the file content into the editor, (2) keystrokes in the editor update the editor buffer, (3) Cmd+S (macOS) / Ctrl+S (Linux/Windows) calls the save_file IPC command to write the current editor content to disk. This is the completion of the M0 walking skeleton. Implement an end-to-end test that exercises all nine steps from the press release journey using Tauri WebDriver or a headless script.",
      "priority": "must",
      "category": "scaffolding",
      "steps": [
        "Add @tauri-apps/api as an npm dependency for the invoke() and dialog.open() APIs",
        "Add a toolbar or menu with an Open button that calls dialog.open({filters: [{name: 'Markdown', extensions: ['md']}]}) and then invoke('open_file', {path})",
        "On successful open_file response, call editor.dispatch() to replace the editor content with the returned text",
        "Wire a keydown listener for Cmd+S / Ctrl+S that calls editor.state.doc.toString() and invoke('save_file', {path, content})",
        "Update the save_file IPC command in Rust to accept the content string from JS rather than reading from AppState (simpler for M0)",
        "Wire the native menu File > Open and File > Save to the same JS handlers",
        "Write an end-to-end test: create a temp .md file, launch app, open file, type text, save, read the file from disk, assert new text is present"
      ],
      "acceptanceCriteria": [
        {
          "text": "A named end-to-end test exists that launches the bridge, opens a temp file, applies an edit, saves, reopens, and asserts the new content is on disk",
          "verify": "cargo test --workspace e2e_open_edit_save_reopen 2>&1 | grep -qE 'test result: ok\\. [1-9]'"
        },
        {
          "text": "cargo test --workspace passes every test (no ignored, no filtered failures)",
          "verify": "cargo test --workspace 2>&1 | grep -qE 'test result: ok' && ! cargo test --workspace 2>&1 | grep -qE 'FAILED|test result: FAILED'"
        },
        {
          "text": "The Tauri app builds successfully for macOS",
          "verify": "cd src-tauri && cargo build --release 2>&1 | tail -5 | grep -qE 'Finished|Compiling'"
        }
      ],
      "scope": [
        "src/**",
        "src-tauri/src/**"
      ],
      "dependsOn": [
        "FEAT-003",
        "FEAT-004"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-006",
      "title": "M0-baseline: Capture performance baseline and lock regression gate",
      "intent": "docs/baseline.json exists, is committed, and contains real measured values for all six metrics from PRODUCT.md §7.1.5 on a documented reference machine. The CI job fails if a subsequent commit regresses any metric by more than 10%. Until this item is done, no Phase 1 or §7.2 feature item may be marked readyForSprint.",
      "description": "After M0-open-save-loop passes, run a measurement pass on a documented reference machine and capture: cold startup to editable cursor, open 100-line file, open 10k-line file, keystroke to on-screen, save round-trip, and memory footprint after 1 min editing a 10k-line file. Write the results plus machine metadata (make, model, OS version) into docs/baseline.json. Add a CI step that re-runs the measurements on every PR and fails if any metric degrades by more than 10%. Document the reference machine in docs/baseline.json — these numbers are not portable and must not be compared across machines.",
      "priority": "must",
      "category": "scaffolding",
      "steps": [
        "Write a shell or Rust script that launches the app, performs each of the six operations with timing, and outputs JSON",
        "Run the script on the reference machine and capture the six values",
        "Write docs/baseline.json with the six metrics plus machine metadata (hostname, OS version, CPU, RAM)",
        "Add a CI job step that runs the measurement script and parses the output",
        "In the CI job, compare each measured value against docs/baseline.json; fail the job if any value exceeds 110% of the baseline",
        "Commit docs/baseline.json with a clear commit message indicating this is the M0 baseline",
        "Update the CI workflow to include the regression gate job on every PR targeting main"
      ],
      "acceptanceCriteria": [
        {
          "text": "docs/baseline.json exists and contains all six metric fields from PRODUCT.md §7.1.5",
          "verify": "test -f docs/baseline.json && python3 -c \"import json; d=json.load(open('docs/baseline.json')); assert 'cold_startup_ms' in d or len(d) >= 6\""
        },
        {
          "text": "docs/baseline.json includes machine metadata (os_version, cpu, or similar fields)",
          "verify": "test -f docs/baseline.json && python3 -c \"import json; d=json.load(open('docs/baseline.json')); assert any(k in d for k in ['machine','os','cpu','hostname'])\""
        },
        {
          "text": "The GitHub Actions workflow includes a regression gate job or step that references docs/baseline.json",
          "verify": "grep -r 'baseline.json' .github/workflows/"
        }
      ],
      "scope": [
        "docs/baseline.json",
        ".github/workflows/**",
        "scripts/**"
      ],
      "dependsOn": [
        "FEAT-005"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-007",
      "title": "Phase0-tree-sitter: Integrate tree-sitter-markdown into em-core",
      "intent": "em-core can parse any markdown string into a typed AST using tree-sitter-markdown (split_parser branch). The parse result is a Rust struct tree, not a raw tree-sitter CST. Every downstream feature (formatting, doctor, WYSIWYM decorations) reads from this AST — no component parses markdown ad-hoc.",
      "description": "Integrate tree-sitter-markdown (the split_parser branch, which separates block and inline grammars) into em-core as the single parser. Port the AST node types to Rust enums that map to the tree-sitter node kinds. The AST types must cover: headings, paragraphs, code blocks (fenced and indented), lists and list items, blockquotes, tables (GFM), links, images, emphasis, strong, inline code, thematic breaks, and frontmatter (YAML block detection). Provide a parse(text: &str) -> SyntaxTree function in em-core. Do NOT use the String-based Document from M0 for the AST — the parser operates on a &str input and returns an owned tree. Reference: reference/EMParser/ in the Swift prototype for node type inspiration (algorithm reference only — no Swift code transfers).",
      "priority": "must",
      "category": "core",
      "steps": [
        "Add tree-sitter and tree-sitter-markdown as Cargo dependencies (using the split_parser branch via git dependency)",
        "Create em-core/src/ast.rs defining Rust enums for all major node kinds listed in the description",
        "Create em-core/src/parser.rs implementing parse(text: &str) -> SyntaxTree that walks the tree-sitter CST",
        "Map tree-sitter node type strings (e.g. 'atx_heading', 'fenced_code_block') to the Rust AST enums",
        "Handle GFM extensions: table, strikethrough, task list item, autolink",
        "Handle YAML frontmatter: detect a leading '---' block and wrap it in a FrontmatterNode",
        "Write unit tests in em-core/src/parser.rs that parse representative markdown fragments and assert on the resulting AST structure",
        "Run cargo test -p em-core and confirm all new parser tests pass"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test -p em-core passes including parser unit tests for headings, code blocks, lists, tables, and links",
          "verify": "cargo test -p em-core 2>&1 | grep -qE 'test result: ok'"
        },
        {
          "text": "em-core/src/ast.rs defines Rust enums covering the major node kinds",
          "verify": "grep -q 'enum.*Node\\|NodeKind' em-core/src/ast.rs"
        },
        {
          "text": "em-core/src/parser.rs implements a pub fn parse(text: &str) function",
          "verify": "grep -q 'pub fn parse' em-core/src/parser.rs"
        },
        {
          "text": "tree-sitter-markdown appears in em-core/Cargo.toml as a dependency",
          "verify": "grep -q 'tree-sitter' em-core/Cargo.toml"
        }
      ],
      "scope": [
        "em-core/src/ast.rs",
        "em-core/src/parser.rs",
        "em-core/Cargo.toml"
      ],
      "dependsOn": [
        "FEAT-006"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-008",
      "title": "Phase0-piece-table: Replace String doc model with piece table",
      "intent": "The Document struct in em-core uses a piece table (red-black tree of pieces over original + add buffers) instead of a plain String. All four public API functions (open_file, edit, save_file, current_text) work identically from the caller's perspective. The M0 integration tests still pass. A 100MB file can be opened and edited without copying the entire file into a new String on every keystroke.",
      "description": "Replace the String-backed Document with a piece table implementation as specified in ARCHITECTURE.md. The piece table stores pieces in a red-black tree keyed by cumulative byte offset. Each piece references either the original buffer (read from disk, never mutated) or the append-only add buffer. Maintain an incremental line-starts index (Vec<usize> of byte offsets) updated on every edit. The public API surface (open_file, edit, save_file, current_text) must remain unchanged. Compaction triggers when piece count exceeds 10x line count or on save. All previous em-core integration tests must continue to pass after this replacement. Reference: ARCHITECTURE.md §Document Model for the full spec.",
      "priority": "must",
      "category": "core",
      "steps": [
        "Create em-core/src/piece_table.rs with the PieceTable struct, Buffer enum (Original, Add), and Piece struct",
        "Implement a red-black tree (use the 'rbtree' or 'btreemap' crate, or implement from scratch) keyed by cumulative offset",
        "Implement insert(offset, text) and delete(offset, length) on PieceTable using piece splitting and insertion",
        "Implement iter_text() -> impl Iterator<Item=&str> that yields text slices from pieces in order",
        "Implement current_text() -> String by collecting iter_text()",
        "Implement the line-starts index: a sorted Vec<usize> updated incrementally on each edit",
        "Implement compaction: when piece_count > 10 * line_count or on save, rewrite into a fresh buffer",
        "Replace the String field in Document with PieceTable",
        "Run cargo test -p em-core and confirm all tests pass, including the M0 round-trip integration test"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test -p em-core passes including the M0 integration test (open, edit, save, reopen, assert)",
          "verify": "cargo test -p em-core 2>&1 | grep -qE 'test result: ok'"
        },
        {
          "text": "em-core/src/piece_table.rs exists and defines a PieceTable struct",
          "verify": "test -f em-core/src/piece_table.rs && grep -q 'struct PieceTable' em-core/src/piece_table.rs"
        },
        {
          "text": "Document no longer holds a plain String — it delegates to PieceTable",
          "verify": "grep -q 'PieceTable' em-core/src/lib.rs"
        },
        {
          "text": "cargo bench or a timing test shows editing a 100k-char document does not copy more than one piece worth of bytes",
          "verify": "cargo test -p em-core 2>&1 | grep -qE 'test result: ok'"
        }
      ],
      "scope": [
        "em-core/src/piece_table.rs",
        "em-core/src/lib.rs",
        "em-core/Cargo.toml"
      ],
      "dependsOn": [
        "FEAT-006"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-009",
      "title": "Phase0-formatting-engine: Rule-based formatting engine in Rust",
      "intent": "em-core can format any markdown document in under 50ms for a 10k-line file, applying the five core rules. Every rule is implemented from scratch in Rust using the EMFormatter Swift algorithms as reference. No Swift code transfers. The output is a Vec<Mutation> describing byte-range replacements — the caller (CodeMirror bridge) applies them, the engine does not mutate documents directly.",
      "description": "Implement the formatting engine in em-core/src/formatter.rs. The engine runs five ordered rules ported from the algorithm logic in reference/EMFormatter/: (1) list continuation — auto-indent on Enter at list item, (2) table alignment — normalize column widths and separator row dashes, (3) heading spacing — ensure blank line before each heading unless at document start, (4) blank line separation — ensure blank line between paragraphs and block elements, (5) trailing whitespace trim — remove trailing spaces from all lines. The engine takes a parsed SyntaxTree (from FEAT-007) and the document text, and returns Vec<Mutation> where Mutation = {offset: usize, delete: usize, insert: String}. Rules are applied in order; later rules see the pre-mutation offsets (mutations are not applied in-place during formatting). A public format(tree: &SyntaxTree, text: &str) -> Vec<Mutation> function is the entry point. Reference the Swift algorithms only — do not call swift build.",
      "priority": "must",
      "category": "core",
      "steps": [
        "Create em-core/src/formatter.rs with a Mutation struct {offset, delete, insert} and a format() entry point",
        "Implement Rule 1: list continuation — detect list items in the AST, compute correct indentation for next line",
        "Implement Rule 2: table alignment — scan table nodes, compute max column widths, pad cells with spaces",
        "Implement Rule 3: heading spacing — scan heading nodes, check preceding byte for blank line, emit insert mutation if missing",
        "Implement Rule 4: blank line separation — scan block elements, ensure blank line between adjacent paragraphs and block elements",
        "Implement Rule 5: trailing whitespace — scan each line via the line-starts index, emit delete mutation for trailing spaces",
        "Write unit tests for each rule in em-core/src/formatter.rs covering at least the happy path and one edge case per rule",
        "Run cargo test -p em-core and confirm all formatter tests pass"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test -p em-core passes including formatter unit tests for all five rules",
          "verify": "cargo test -p em-core 2>&1 | grep -qE 'test result: ok'"
        },
        {
          "text": "em-core/src/formatter.rs exists and exports pub fn format()",
          "verify": "test -f em-core/src/formatter.rs && grep -q 'pub fn format' em-core/src/formatter.rs"
        },
        {
          "text": "The format() function returns Vec<Mutation> (not a mutated document)",
          "verify": "grep -q 'Vec.*Mutation\\|Mutation.*Vec' em-core/src/formatter.rs"
        },
        {
          "text": "Formatting a 10k-line document completes in under 50ms (per PRODUCT.md D-PERF-1)",
          "verify": "cargo test -p em-core --test perf 2>&1 | grep -qE 'test result: ok' || cargo test -p em-core 2>&1 | grep -qE 'test result: ok'"
        }
      ],
      "scope": [
        "em-core/src/formatter.rs",
        "em-core/src/lib.rs"
      ],
      "dependsOn": [
        "FEAT-006",
        "FEAT-007",
        "FEAT-008"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-010",
      "title": "Phase0-commonmark-ci: CommonMark spec suite wired into CI with skip-list",
      "intent": "Every CI run reports exactly how many CommonMark spec tests pass, how many are in the documented skip-list, and zero unexplained failures. Any test that fails without a skip-list entry breaks the build. Known tree-sitter divergences are named, documented, and tracked for upstream contribution.",
      "description": "Wire the official CommonMark spec test suite (spec.json from the commonmark-spec repo) into the em-core test harness. The test runner parses each spec test's markdown input through em-core's parse() function, renders the output, and compares against the expected HTML. Maintain an explicit skip-list at em-core/tests/commonmark_skip.json — each entry must include the spec test number, the failing input fragment, and a one-sentence reason (e.g. 'tree-sitter lazy continuation divergence, see issue #N'). CI fails if any test fails and is NOT in the skip-list. CI also fails if a skip-listed test starts passing (a signal to remove it from the list). Document the current pass rate in docs/commonmark_status.md.",
      "priority": "must",
      "category": "quality",
      "steps": [
        "Download or vendor spec.json from the commonmark-spec repository into em-core/tests/fixtures/",
        "Write em-core/tests/commonmark.rs that reads spec.json and runs each test through parse()",
        "Implement a minimal HTML renderer in the test harness (or use a reference renderer) to produce output for comparison",
        "Add em-core/tests/commonmark_skip.json — start with an empty array, add entries as failures are discovered",
        "Update the test runner to fail on any unskipped failure and warn on any skip-listed test that now passes",
        "Add the commonmark test job to the GitHub Actions workflow",
        "Run the suite, identify all failures, populate the skip-list with documented reasons",
        "Create docs/commonmark_status.md with the current pass/fail/skipped counts"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test --workspace runs the CommonMark spec suite and produces a summary line with pass/skip/fail counts",
          "verify": "cargo test --workspace -- commonmark 2>&1 | grep -qiE 'pass|skip|commonmark'"
        },
        {
          "text": "em-core/tests/commonmark_skip.json exists with at least structure (can be empty array for projects at 100%)",
          "verify": "test -f em-core/tests/commonmark_skip.json"
        },
        {
          "text": "docs/commonmark_status.md exists documenting the current pass rate",
          "verify": "test -f docs/commonmark_status.md"
        },
        {
          "text": "The GitHub Actions workflow includes a step that runs the CommonMark test suite",
          "verify": "grep -r 'commonmark' .github/workflows/"
        }
      ],
      "scope": [
        "em-core/tests/**",
        "docs/commonmark_status.md",
        ".github/workflows/**"
      ],
      "dependsOn": [
        "FEAT-006",
        "FEAT-007"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    }
  ]
}
