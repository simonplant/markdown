{
  "description": "Feature backlog",
  "items": [
    {
      "id": "FEAT-004",
      "title": "M0-editor: CodeMirror 6 plain-text editor in the webview",
      "intent": "The webview displays a CodeMirror 6 editor instance. The user can type characters and see them appear immediately in the editor. No IPC yet — this item proves CodeMirror 6 is mounted, receives keystrokes, and maintains a local buffer.",
      "description": "Mount CodeMirror 6 in the Tauri webview with a minimal plain-text configuration. No markdown mode, no syntax highlighting, no decorations — raw text input only. The editor must be focusable, must accept keyboard input, and must maintain its content in a local JS buffer. Wire up a basic TypeScript/JS module that imports CodeMirror 6 packages via npm and initializes an EditorView. IPC bridge integration comes in M0-open-save-loop — this item ends when the editor is visible and accepts input.",
      "priority": "must",
      "category": "scaffolding",
      "steps": [
        "Add CodeMirror 6 packages as npm dependencies: @codemirror/view, @codemirror/state",
        "Create src/editor.ts (or .js) that imports EditorView and EditorState from @codemirror/view and @codemirror/state",
        "Initialize an EditorView with a minimal setup (basicSetup from codemirror package, or manual extensions list)",
        "Mount the EditorView into a DOM element on index.html load",
        "Verify the editor is visible in the webview and accepts keyboard input",
        "Export a getContent() function from editor.ts that returns the current editor string content",
        "Wire the build system (Vite or equivalent) to bundle the TypeScript/JS for the Tauri webview"
      ],
      "acceptanceCriteria": [
        {
          "text": "npm install and npm run build both exit 0",
          "verify": "npm install && npm run build"
        },
        {
          "text": "Built output includes an index.html entry point",
          "verify": "test -f dist/index.html"
        },
        {
          "text": "The built JS bundle contains CodeMirror EditorView code",
          "verify": "grep -rql EditorView dist/"
        },
        {
          "text": "The built bundle does NOT contain @codemirror/lang-markdown — M0 is plain-text-only",
          "verify": "! grep -rqlE 'lang-markdown|markdownLanguage' dist/"
        }
      ],
      "scope": [
        "src/**",
        "package.json",
        "vite.config.*",
        "tsconfig.*"
      ],
      "dependsOn": [
        "FEAT-002"
      ],
      "status": "done",
      "passes": false,
      "readyForSprint": true,
      "resolved_at": "2026-04-12T05:32:33Z"
    },
    {
      "id": "FEAT-007",
      "title": "Phase0-tree-sitter: Integrate tree-sitter-markdown into em-core",
      "intent": "em-core can parse any markdown string into a typed AST using tree-sitter-markdown (split_parser branch). The parse result is a Rust struct tree, not a raw tree-sitter CST. Every downstream feature (formatting, doctor, WYSIWYM decorations) reads from this AST — no component parses markdown ad-hoc.",
      "description": "Integrate tree-sitter-markdown (the split_parser branch, which separates block and inline grammars) into em-core as the single parser. Port the AST node types to Rust enums that map to the tree-sitter node kinds. The AST types must cover: headings, paragraphs, code blocks (fenced and indented), lists and list items, blockquotes, tables (GFM), links, images, emphasis, strong, inline code, thematic breaks, and frontmatter (YAML block detection). Provide a parse(text: &str) -> SyntaxTree function in em-core. Do NOT use the String-based Document from M0 for the AST — the parser operates on a &str input and returns an owned tree. Reference for algorithm inspiration (not code): the legacy Swift files in reference/ (TreeSitterConversion.swift and the EMFormatter/EMDoctor rule files). Note that reference/ does not contain an EMParser directory — use TreeSitterConversion.swift as the tree-sitter node-type mapping reference.",
      "priority": "must",
      "category": "core",
      "steps": [
        "Add tree-sitter and tree-sitter-markdown as Cargo dependencies (using the split_parser branch via git dependency)",
        "Create em-core/src/ast.rs defining Rust enums for all major node kinds listed in the description",
        "Create em-core/src/parser.rs implementing parse(text: &str) -> SyntaxTree that walks the tree-sitter CST",
        "Map tree-sitter node type strings (e.g. 'atx_heading', 'fenced_code_block') to the Rust AST enums",
        "Handle GFM extensions: table, strikethrough, task list item, autolink",
        "Handle YAML frontmatter: detect a leading '---' block and wrap it in a FrontmatterNode",
        "Write unit tests in em-core/src/parser.rs that parse representative markdown fragments and assert on the resulting AST structure",
        "Run cargo test -p em-core and confirm all new parser tests pass"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test -p em-core --test parser passes with at least 8 distinct test cases (heading, paragraph, fenced code, bullet list, ordered list, link, strong, emphasis)",
          "verify": "cargo test -p em-core --test parser 2>&1 | grep -qE 'test result: ok\\. ([8-9]|[1-9][0-9])'"
        },
        {
          "text": "cargo tree lists tree-sitter and tree-sitter-md (or equivalent) as direct dependencies of em-core",
          "verify": "cargo tree -p em-core 2>&1 | grep -qE 'tree-sitter( |-md)'"
        },
        {
          "text": "parse() returns a typed AST with at least the major node kinds — not a raw tree-sitter cursor",
          "verify": "cargo test -p em-core --test parser returns_typed_ast 2>&1 | grep -qE 'test result: ok\\. [1-9]'"
        },
        {
          "text": "cargo test --workspace exits 0 with no failures after integrating the parser",
          "verify": "cargo test --workspace 2>&1 | grep -qE 'test result: ok' && ! cargo test --workspace 2>&1 | grep -qE 'FAILED'"
        }
      ],
      "scope": [
        "em-core/src/ast.rs",
        "em-core/src/parser.rs",
        "em-core/Cargo.toml"
      ],
      "dependsOn": [
        "FEAT-006"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-008",
      "title": "Phase0-engine-decision: Decide and implement the post-M0 document model",
      "intent": "The document model engine decision is made against real baseline numbers, not speculation. ARCHITECTURE.md §Document Model lists three candidates (A piece table, B rope, C keep String). The FEAT-006 baseline.json exists and is authoritative. This item runs the three candidates against the same measurement script, picks the winner on the defensible criterion (the one that does not regress any baseline metric by more than 10% AND has the lowest complexity cost), and lands the chosen implementation. The four em-core public functions (open_file, edit, save_file, current_text) remain unchanged from M0.",
      "description": "Do not assume the answer. Run the measurement script from FEAT-006 against three implementations of em-core::Document: (A) a piece table with a balanced BST of pieces over original + add buffers, (B) a rope using the ropey crate, (C) the M0 String as a control. Commit the three measurement runs to docs/engine-comparison.json. Pick the implementation that does not regress any baseline metric by more than 10% AND has the lowest complexity cost (fewest added lines, no new unsafe, no platform-specific code). Default to C (keep String) unless A or B demonstrably beats it on a metric that matters for the product scope. Write a one-paragraph decision record in docs/engine-decision.md explaining the chosen candidate and the measured numbers that justified it. Replace em-core/src/lib.rs::Document with the chosen implementation. All M0 integration tests must continue to pass unchanged.",
      "priority": "must",
      "category": "core",
      "steps": [
        "Read docs/baseline.json (committed by FEAT-006) and the measurement script it references",
        "Create three temporary branches or modules: engine_a_piece_table, engine_b_rope, engine_c_string_control",
        "Implement candidate A (piece table) in engine_a_piece_table — pieces in a BTreeMap<usize, Piece>, Original and Add buffers, incremental line-starts",
        "Implement candidate B (rope) in engine_b_rope using the ropey crate",
        "Leave candidate C as the existing String implementation",
        "Run the FEAT-006 measurement script against each candidate on the canonical ubuntu-latest environment, with N>=5 runs each",
        "Write docs/engine-comparison.json with the three candidates median numbers for each metric",
        "Pick the winner per the decision criterion in the description",
        "Write docs/engine-decision.md explaining the choice",
        "Replace em-core/src/lib.rs::Document with the chosen candidate",
        "Run cargo test --workspace and confirm all M0 tests still pass",
        "Delete the two losing candidate modules"
      ],
      "acceptanceCriteria": [
        {
          "text": "docs/engine-comparison.json exists and contains median measurements for all three candidates on all six baseline metrics",
          "verify": "test -f docs/engine-comparison.json && jq -e '.candidates | length == 3 and (all(.metrics | length >= 6))' docs/engine-comparison.json"
        },
        {
          "text": "docs/engine-decision.md exists and names the chosen candidate with a justification referencing measured numbers",
          "verify": "test -f docs/engine-decision.md && grep -qiE 'chosen|winner|decision' docs/engine-decision.md"
        },
        {
          "text": "cargo test --workspace passes — every M0 integration test still green after the engine swap",
          "verify": "cargo test --workspace 2>&1 | grep -qE 'test result: ok\\. [1-9]' && ! cargo test --workspace 2>&1 | grep -qE 'FAILED'"
        },
        {
          "text": "The measurement run on the chosen candidate does not regress any baseline.json metric by more than 10%",
          "verify": "cargo run -p em-core --example baseline_check 2>&1 | grep -qiE 'no regression|within budget'"
        }
      ],
      "scope": [
        "em-core/src/piece_table.rs",
        "em-core/src/lib.rs",
        "em-core/Cargo.toml"
      ],
      "dependsOn": [
        "FEAT-006"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-009",
      "title": "Phase0-formatting-engine: Rule-based formatting engine in Rust",
      "intent": "em-core can format any markdown document in under 50ms for a 10k-line file, applying the five core rules. Every rule is implemented from scratch in Rust using the EMFormatter Swift algorithms as reference. No Swift code transfers. The output is a Vec<Mutation> describing byte-range replacements — the caller (CodeMirror bridge) applies them, the engine does not mutate documents directly.",
      "description": "Implement the formatting engine in em-core/src/formatter.rs. The engine runs five ordered rules ported from the algorithm logic in reference/EMFormatter/: (1) list continuation — auto-indent on Enter at list item, (2) table alignment — normalize column widths and separator row dashes, (3) heading spacing — ensure blank line before each heading unless at document start, (4) blank line separation — ensure blank line between paragraphs and block elements, (5) trailing whitespace trim — remove trailing spaces from all lines. The engine takes a parsed SyntaxTree (from FEAT-007) and the document text, and returns Vec<Mutation> where Mutation = {offset: usize, delete: usize, insert: String}. Rules are applied in order; later rules see the pre-mutation offsets (mutations are not applied in-place during formatting). A public format(tree: &SyntaxTree, text: &str) -> Vec<Mutation> function is the entry point. Reference the Swift algorithms only — do not call swift build.",
      "priority": "must",
      "category": "core",
      "steps": [
        "Create em-core/src/formatter.rs with a Mutation struct {offset, delete, insert} and a format() entry point",
        "Implement Rule 1: list continuation — detect list items in the AST, compute correct indentation for next line",
        "Implement Rule 2: table alignment — scan table nodes, compute max column widths, pad cells with spaces",
        "Implement Rule 3: heading spacing — scan heading nodes, check preceding byte for blank line, emit insert mutation if missing",
        "Implement Rule 4: blank line separation — scan block elements, ensure blank line between adjacent paragraphs and block elements",
        "Implement Rule 5: trailing whitespace — scan each line via the line-starts index, emit delete mutation for trailing spaces",
        "Write unit tests for each rule in em-core/src/formatter.rs covering at least the happy path and one edge case per rule",
        "Run cargo test -p em-core and confirm all formatter tests pass"
      ],
      "acceptanceCriteria": [
        {
          "text": "cargo test -p em-core --test formatter passes with explicit test cases for list continuation, table alignment, heading spacing, blank-line separation, and trailing whitespace trim (5+ tests)",
          "verify": "cargo test -p em-core --test formatter 2>&1 | grep -qE 'test result: ok\\. ([5-9]|[1-9][0-9])'"
        },
        {
          "text": "format() is idempotent — running it twice on the same input produces the same output",
          "verify": "cargo test -p em-core --test formatter format_is_idempotent 2>&1 | grep -qE 'test result: ok\\. [1-9]'"
        },
        {
          "text": "format() does not change the semantic content — only whitespace, list markers, and alignment are adjusted",
          "verify": "cargo test -p em-core --test formatter format_preserves_semantics 2>&1 | grep -qE 'test result: ok\\. [1-9]'"
        },
        {
          "text": "cargo test --workspace exits 0 with no failures and no baseline regression",
          "verify": "cargo test --workspace 2>&1 | grep -qE 'test result: ok' && ! cargo test --workspace 2>&1 | grep -qE 'FAILED'"
        }
      ],
      "scope": [
        "em-core/src/formatter.rs",
        "em-core/src/lib.rs"
      ],
      "dependsOn": [
        "FEAT-006",
        "FEAT-007",
        "FEAT-008"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    },
    {
      "id": "FEAT-010",
      "title": "Phase0-commonmark-ci: CommonMark spec suite wired into CI with skip-list",
      "intent": "Every CI run reports exactly how many CommonMark spec tests pass, how many are in the documented skip-list, and zero unexplained failures. Any test that fails without a skip-list entry breaks the build. Known tree-sitter divergences are named, documented, and tracked for upstream contribution.",
      "description": "Wire the official CommonMark spec test suite (spec.json from the commonmark-spec repo) into the em-core test harness. The test runner parses each spec test's markdown input through em-core's parse() function, renders the output, and compares against the expected HTML. Maintain an explicit skip-list at em-core/tests/commonmark_skip.json — each entry must include the spec test number, the failing input fragment, and a one-sentence reason (e.g. 'tree-sitter lazy continuation divergence, see issue #N'). CI fails if any test fails and is NOT in the skip-list. CI also fails if a skip-listed test starts passing (a signal to remove it from the list). Document the current pass rate in docs/commonmark_status.md.",
      "priority": "must",
      "category": "core",
      "steps": [
        "Download or vendor spec.json from the commonmark-spec repository into em-core/tests/fixtures/",
        "Write em-core/tests/commonmark.rs that reads spec.json and runs each test through parse()",
        "Implement a minimal HTML renderer in the test harness (or use a reference renderer) to produce output for comparison",
        "Add em-core/tests/commonmark_skip.json — start with an empty array, add entries as failures are discovered",
        "Update the test runner to fail on any unskipped failure and warn on any skip-listed test that now passes",
        "Add the commonmark test job to the GitHub Actions workflow",
        "Run the suite, identify all failures, populate the skip-list with documented reasons",
        "Create docs/commonmark_status.md with the current pass/fail/skipped counts"
      ],
      "acceptanceCriteria": [
        {
          "text": "em-core/tests/fixtures/spec.json exists (CommonMark 0.31.2 spec examples)",
          "verify": "test -f em-core/tests/fixtures/spec.json && jq -e '. | length > 600' em-core/tests/fixtures/spec.json"
        },
        {
          "text": "em-core/tests/commonmark_skip.json exists listing known divergences, each with a reason",
          "verify": "test -f em-core/tests/commonmark_skip.json && jq -e 'all(.[]; .reason != null)' em-core/tests/commonmark_skip.json"
        },
        {
          "text": "cargo test -p em-core --test commonmark runs the spec suite and reports pass/fail/skip counts",
          "verify": "cargo test -p em-core --test commonmark 2>&1 | grep -qiE 'commonmark.*(pass|fail|skip).*[0-9]+'"
        },
        {
          "text": "docs/commonmark_status.md exists and documents the current pass rate as a committed number",
          "verify": "test -f docs/commonmark_status.md && grep -qE '[0-9]+%|[0-9]+/[0-9]+' docs/commonmark_status.md"
        },
        {
          "text": "A GitHub Actions workflow job runs the commonmark test and fails if the pass rate drops below the committed number",
          "verify": "grep -rqE 'commonmark' .github/workflows/ && yq '.jobs | to_entries | .[] | select(.value | tostring | test(\"commonmark\"))' .github/workflows/*.yml 2>/dev/null | grep -q ."
        }
      ],
      "scope": [
        "em-core/tests/**",
        "docs/commonmark_status.md",
        ".github/workflows/**"
      ],
      "dependsOn": [
        "FEAT-006",
        "FEAT-007"
      ],
      "status": "todo",
      "passes": false,
      "readyForSprint": false
    }
  ]
}
