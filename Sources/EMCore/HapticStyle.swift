/// Haptic vocabulary for the app per [A-062].
/// Each case maps to a UIImpactFeedbackGenerator style.
public enum HapticStyle: Sendable {
    case listContinuation
    case doctorFixApplied
    case aiAccepted
    case autoSaveConfirm
    case toggleView
}
